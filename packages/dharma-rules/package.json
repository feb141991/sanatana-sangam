{
  "name": "@sangam/dharma-rules",
  "version": "0.1.0",
  "description": "Golden-fixture harness and calendar correctness tests for the Shoonaya calendar engine",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:calendar": "vitest run --reporter=verbose",
    "verify:harness": "vitest run harness/ --reporter=verbose",
    "generate:snapshots": "npx tsx scripts/generate-snapshots.ts"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "typescript": "^5.4.0",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "@sangam/panchang-engine": "*",
    "@supabase/supabase-js": "^2.112.3",
    "dotenv": "^17.4.2"
  }
}
