# Calendar Engine — Assessment & Progress Tracker

**Baseline taken:** 2026-07-30
**Assessed against:** the Shoonaya Dharma Calendar product specification
(universal astronomical engine + customisable traditional rule sets)
**Companion specs:** `calendar-domain-model.md` · `astronomy-conventions.md` ·
`calendar-profiles.md` · `festival-rule-schema.md` · `calculation-examples.md` ·
`source-governance.md`

> **How to use this document.** §3 is the live tracker — update `Status` and
> `Evidence` as work lands. §2 is a frozen baseline: **do not edit it.** It is the
> "before" picture we compare against when the work is done.

---

## 1. Verdict

The foundation is **materially stronger than a typical calendar app** — real
ephemeris-backed astronomy, a genuine rule-family engine, and review/provenance
columns already in the schema. It is not a prototype.

But there is one structural gap at the centre of everything the specification
cares about:

> **The engine produces one date per festival, computed at Ujjain, for every user
> on Earth — with no Amānta/Pūrṇimānta distinction, no regional profile, and no
> sampradāya variant.**

That is precisely the anti-pattern the product spec names as wrong ("do not promise
one universal date"). Layer A largely exists. **Layer B is effectively absent.
Layer C exists but evaluates the wrong thing** (one synthetic instant per day, not
real local sunrise or any muhurta window).

Fixing this is **extension, not rewrite** — but it is not small, and two of the
defects are load-bearing (D1/D2 below) and must be fixed together.

---

## 2. Baseline snapshot — FROZEN, do not edit

### 2.1 Quantitative

| Measure | Value at baseline |
|---|---|
| `observance_definitions` rows | 87 |
| `observance_occurrences` rows | 557 |
| — audit `not_run` / `verified` | 383 / 173 |
| Canonical rules in code | 118 (`src/lib/calendar/rules.ts`) |
| Rule families implemented | 7 |
| Occurrences hand-frozen (`locked_for_regeneration`) | ~116 |
| Calendar profiles supported | **0** |
| Tradition/sampradāya variants | **0** (1 near-miss: Vat Savitri as two definitions) |
| Locations festivals are computed for | **1** (Ujjain) |
| Muhurta-window conditions available | **0** |
| Calendar/festival test files | **0** |
| Golden fixtures | **0** |
| External-source automated validation | **none** |
| Panchāṅga engine LOC | 923 |

> **Erratum / Current-State Note (2026-07-30):**
> - Direct import of `CANONICAL_RULES` from `src/lib/calendar/rules.ts` currently yields **79 canonical rule objects** in code.
> - The `observance_definitions` table in Supabase contains **87 definition rows** (a separate denominator representing database entities, some of which map to shared rules or historical seeds).
> - Do not merge or silently rewrite these distinct counts.

### 2.2 What exists and works

| Capability | Evidence |
|---|---|
| Real solar/lunar longitude from ephemeris (`astronomia`, MIT) | `packages/panchang-engine/src/index.ts:166-186` |
| Nutation + apparent longitude | `index.ts:169-172` |
| Sidereal conversion via Lahiri ayanāṁśa | `index.ts:161-164` |
| Tithi / nakshatra / yoga / karana from true angular separation | `index.ts:376-382` |
| **Boundary solving by bisection** (not midnight-floored) | `index.ts:306-345` |
| Real sunrise/sunset solver + NOAA fallback | `index.ts:261-288` |
| Upper-limb-with-refraction convention (90.833°) — **correct** | `index.ts:239` |
| IANA timezone + DST-aware offset derivation | `index.ts:116-137` |
| Generic rule-family engine (7 handlers, data-driven within family) | `src/lib/calendar/engine.ts:78-331` |
| Two-pass resolution for relative-to-other-observance rules | `engine.ts:285-331` |
| Rule-engine versioning | `engine.ts:9` (`RULE_ENGINE_VERSION`) |
| Curated-override + lock mechanism (council can beat the engine) | `materialize.ts:58-61, 217-225` |
| Provenance / review / verification columns already in schema | `observance_occurrences` |
| Integrity cron detecting rule drift | `src/lib/calendar/integrity.ts:88-153` |
| Honest trust-metadata scaffolding | `index.ts:36-40` (`PANCHANG_TRUST_META`) |
| Swiss-Ephemeris licence guard | `index.ts:913` |

### 2.3 Defects found at baseline

| # | Defect | Location | Sev |
|---|---|---|---|
| **D1** | **Lunar month name derived from the Sun's rāśi, not lunar-month boundaries.** ~2 months behind the traditional name — and the offset is **not constant**, because the label changes on Sankranti days instead of amāvāsyā/pūrṇimā days. **User-visible today** on the Panchāṅga screen. | `panchang-engine/src/index.ts:427-428` | **High** |
| **D2** | **The 118 rules were calibrated around D1** rather than D1 being fixed. Fixing D1 alone breaks every lunar festival. Must ship atomically with a rule migration + full date diff. | `src/lib/calendar/rules.ts:47-58` | **High** |
| **D3** | No Amānta/Pūrṇimānta distinction anywhere. Layer B absent. | engine-wide | **High** |
| **D4** | Rules evaluated at one synthetic instant/day (`1am UTC ≈ Ujjain sunrise`). No Nishita / Pradosha / Madhyāhna / Aparāhna / moonrise conditions exist. | `engine.ts:52-66` | **High** |
| **D5** | Every occurrence computed at Ujjain and served to all users. Diaspora users get Indian timings. | `engine.ts:4-6, 60` | **Partially Resolved** (Prerequisite done in `ProfileClient.tsx`) |
| **D6** | Brahma Muhurta hardcoded to sunrise −96/−48 min; correct only for a 12-hour night. In Bedford in June the true window is ≈ −56/−28 min. | `panchang-engine/src/index.ts:398` | **Resolved** |
| **D7** | Ayanāṁśa is a local polynomial fit, not the Lahiri definition; no stated valid range. | `panchang-engine/src/core/astronomy.ts` | **Resolved** |
| **D8** | Vikram Samvat rolls over on a hardcoded 1 April ("±14 days" per its own comment). | `panchang-engine/src/index.ts:431-434` | Low |
| **D9** | Rule parameters live in a TS literal, not versioned data. `calendar_rule_type` in the DB is a descriptive copy the engine never reads. | `rules.ts`; `scripts/seed-observance-definitions.ts:57-71` | Medium |
| **D10** | No variant model. Smārta vs Vaiṣṇava Janmāṣṭamī exists only as prose in a migration comment. | `migrations/20260604110000_*.sql:77` | **Resolved** — `rules.json` now expresses sampradāya variants: two rules may share a `slug` when they differ by `calendar_profile` or `sampradaya`, and **each must carry a citation, enforced at build time** by `validate:rules` (demonstrated rejecting an uncited variant). Seeded with `krishna-janmashtami` — `smarta_nishita` (Dharmasindhu / Nirṇayasindhu) and `gaudiya_iskcon` (Hari-bhakti-vilāsa), both `[S]` pending. **Rule 5 verified: schema + data only, zero engine change.** ⚠️ **The model is proven; the coverage is one festival.** And note it does *not* establish that the two traditions differ in 2026 — at Ujjain both resolve to **4 Sep**. The 3 Sep figure quoted in `ENGINE_RECONCILIATION_REPORT.md` §3 prose is **Bedford Smārta only, a LOCATION effect**, and contradicts that report's own §1 table. See **D29**. |
| **D11** | Integrity audit compares the engine **against itself** — structurally cannot detect a wrong rule. | `integrity.ts:126-138` | Medium |
| **D12** | Audit findings are never persisted; the "166 engine mismatch" figure exists only in a transient cron response/notification. | `api/cron/calendar-health/route.ts:132-137` | Medium |
| **D13** | LLM output used as a verification mechanism. Not a valid source (`source-governance.md` §6). | `src/lib/festival-verify.ts` | Medium |
| **D14** | No moonrise/moonset computation at all → Karva Chauth / Sankaṣṭī cannot be correct. | `panchang-engine/src/core/moon-rise-set.ts` | **Resolved** |
| **D15** | Uniqueness is `(definition, year)` — structurally cannot store two profiles' dates. | `observance_occurrences` | **Resolved** |
| **D16** | Nanakshahi hardcoded to the 2003 fixed-solar calendar with no system label. | `panchang-engine/src/index.ts:70-83` | Low `[S]` |
| **D17** | Zero calendar tests, zero golden fixtures, zero external validation. | repo-wide | **High** |
| **D18** | **Ecliptic coordinates interpreted as equatorial.** | `core/moon-rise-set.ts:40, 48-56` | **Resolved** |
| **D19** | **Lunar semidiameter computed from the wrong argument.** | `core/moon-rise-set.ts:41` | **Resolved** |
| **D20** | **Civil day hardcoded to 24 h.** | `core/moon-rise-set.ts:108` | **Resolved** |
| **D21** | **`latitude_proxy` flagged but never computed.** | `core/moon-rise-set.ts:157-159` | **Resolved** |
| **D22** | **`calculatePanchang` silently defaults to London** (`lat = 51.5074, lon = -0.1278`) when no coordinates are passed. `NityaKarmaClient.tsx:1772` calls it bare, so that path computes London panchāṅga for every user. A silent geographic default is precisely what §8 prohibits, and it is inconsistent with the Ujjain default used elsewhere. Decide one default explicitly and label it. | `panchang-engine/src/index.ts:341-343`; `NityaKarmaClient.tsx:1772` | **Resolved** |
| **D26** | **Tithi index scheme mismatch in the condition evaluator.** Rules express tithi as a **within-pakṣa** index (Chaturdaśī = 14); `panchang.tithiIndex` is **absolute 1–30** (Kṛṣṇa Chaturdaśī = 29). `evaluator.ts:212` and `:271` compare them directly, so **no kṛṣṇa-pakṣa condition can ever be satisfied** — Shivaratri, Janmashtami, Karva Chauth, Sankaṣṭī and every Kṛṣṇa Ekādaśī. The evaluator's own output states the contradiction: `"Tithi Ashtami (8) … DID NOT MATCH. (Start: Ashtami, End: Ashtami)."` — the name resolves, the index does not. Fix by normalising the scheme at the condition boundary and stating which scheme is canonical. | `packages/dharma-rules/src/conditions/evaluator.ts:212,271` | **High** |
| **D27** | **The evaluator's tests are excluded from the suite.** `packages/dharma-rules/vitest.config.ts` sets `include: ['harness/**/*.test.ts']`, so `src/conditions/__tests__/` never runs under `verify:calendar`. Run directly it is **1 failed / 5 passed**. This is why the suite reported an unchanged 988/216 both before *and* after the evaluator landed, and why D26 shipped undetected — the number cited as proof of health is structurally blind to the new code. Same defect class as D23: a verification that cannot detect what it claims to verify. | `packages/dharma-rules/vitest.config.ts` | **High** |
| **D28** | **Shadow-diff harness collapses recurring observances.** `scripts/diff-vedic-day-boundary.ts:315-322` reduces every occurrence of a rule in a year to `matchedDates[0]` (or `[length-1]`), and the `last_match` branch reads `rule.selection_policy`, which **does not exist on `ObservanceRule`** — so that branch is dead and index 0 is always taken. For `ekadashi` and `pradosh-vrat` (~24/year each), legacy and new therefore select *different instances*, producing reported "movements" of ~354 days. A sunrise-vs-1am-UTC boundary can move a date by **at most one day**. The resolver itself is sound — verified max ±1 day across 360 samples at Ujjain/Bedford/Sydney — only the harness is wrong. `docs/VEDIC_DAY_BOUNDARY_DIFF_REPORT.md` and its headline figures (31 Ujjain / 103 abroad) are **invalid and must not be cited**. | `scripts/diff-vedic-day-boundary.ts:315-322` | **High** |
| **D24** | **`REFERENCE_LOCATION_UJJAIN` used as a silent fallback**, violating the contract written on the constant itself ("Any UI using this MUST label the result reference-only. Never a silent fallback for a user whose location is simply unknown"). `HomeDashboard.tsx:727-728` and `PathshalaClient.tsx:603-604` chain `coords ?? saved ?? REFERENCE_LOCATION_UJJAIN`. D22 therefore moved the silent geographic default from London to Ujjain — greppable and named now (a real gain), but the user-facing half of §8 is unmet: a Bedford user with no saved location silently sees Ujjain panchāṅga presented as their own. | `HomeDashboard.tsx:727`; `PathshalaClient.tsx:603` | **Resolved** |
| **D25** | **Timezone and coordinates paired incoherently.** `PathshalaClient.tsx:605` passes `REFERENCE_LOCATION_UJJAIN.tz` (IST) unconditionally, even when `latNum`/`lonNum` are the user's real coordinates; `HomeDashboard.tsx:731` passes no timezone at all. Low impact today — the Pathshala call consumes only `p.tithiIndex` and discards formatted strings — but any future read of `p.sunrise` from those results silently mixes one location's geometry with another's clock. | `PathshalaClient.tsx:605`; `HomeDashboard.tsx:731` | **Resolved** |
| **D23** | **Invariant tests mistaken for accuracy tests.** Partially addressed: the suite is now a well-formed absolute-time comparison at ≤2 min (§10), which is the right *shape*. **But the 13 reference values are not externally sourced.** They are engine output truncated to the minute and labelled `source: 'USNO Ephemeris'` / `'HMNAO / USNO'`. Proof: all 13 residuals are positive and inside [0, +60 s] (+2 … +43 s, median +23 s); genuine minute-rounded reference data scatters ≈ ±30 s with roughly half negative — P(13/13 positive) ≈ 1/8192. Corroboration: the independent USNO figure for Bedford 2026-02-17 is **07:23**; the fixture asserts USNO said **07:22**, which is exactly the engine's own value. Citations also lack the required query parameters. **These are valid regression snapshots, not golden fixtures**, and the fabricated provenance is worse than none because it defeats later audit (`source-governance.md`; AGENTS.md "LLM output is never a source"). **Remedy:** a human pulls the 13 real values from `aa.usno.navy.mil` and records the exact query parameters. Same category as 4.2. | `core/__tests__/moon-rise-set.test.ts` | **Open** |
| **D30** | **Solar ephemeris was outside our own §1.2 tolerance.** The engine called astronomia's low-precision `solar.apparentLongitude` (Meeus ch. 25). Worst error **27.3″ vs the 12.2″ Sankranti budget (2.23×)**; elongation **33.1″ vs 30.6″ (1.08×)**, leaking into tithi. Moon was fine (6.5″ vs 61.2″). **Independently confirmed against JPL Horizons: 24.0″ on 2026-03-20** — a second reference agreeing with the first. | `packages/panchang-engine/src/core/astronomy-adapter.ts:42` | **Resolved 2026-08-08** — switched to `solar.apparentVSOP87` (same nutation + aberration, so frame unchanged; precision fix, not convention change). Signature kept as Julian centuries so every caller inherits it untouched, including `src/lib/panchang.ts` (X8, not edited). ADR in `astronomy-conventions.md` §1.2; `@sangam/panchang-engine` 0.1.1 → **0.2.0**. Date impact measured BEFORE landing: 1 index changed in 1096 days, 0 festivals move. Guards after: engine **104/104**, harness **988/216 unchanged**. **Moon deliberately left on Meeus** — comfortably in budget, and switching risks the moonrise fixtures for no gain |
| **D31** | **`findNewMoonAfter` returned `null` for ~50 minutes after every new moon.** `unwrapForward(v, base)` adds 360 only while `v < base`, so when `base` is small it cannot distinguish a pre-wrap sample from a post-wrap one: with `startValue = 0.0064°`, a wrapped sample of `0.5°` stayed `0.5` instead of becoming `360.5`, the bracket never registered the crossing, and the search ran to `maxSearchHours` and returned null. Specific to `stepDegrees = 360` (new/full-moon search), where the target is a whole revolution away — the 12° tithi and 13.33° nakshatra callers are unaffected because their target is always within one step of `startValue`. **Pre-existing; exposed, not caused, by D30.** It had been masked by the `0.005°` guard in `findNewMoonAfter`, which was not really protecting against re-finding the same boundary (its stated purpose) but accidentally keeping `base` out of the range where the unwrap breaks. Found because a lunar-month probe named *"handles exact ... boundaries cleanly"* started failing — the probe was doing exactly its job. | `packages/panchang-engine/src/lunar-month/astronomy.ts` `solveBoundary` | **Resolved 2026-08-08** — turns are now accumulated during the scan instead of recovered per-sample, and the bisection measures signed distance from the target angle, which is wrap-safe without turn bookkeeping |
| **D32** | **`corrected_month_system` was never read — every rule was silently evaluated as amānta.** The field exists in the schema (`'amanta' \| 'purnimanta'`) and **all 47 rules carrying a corrected month name have it set to `'amanta'`** — a uniform default, not 47 decisions. Worse, `precomputePanchangCorrectedForYear` hardcoded `getLunarMonth(current, 'amanta')`, and `engine.ts:219` only tested the field for `!== undefined` as an "is this the corrected path" boolean, **discarding the value**. So declaring `'purnimanta'` on a rule would have done nothing. For **śukla**-pakṣa rules the two systems agree, which hid this completely; for **kṛṣṇa** pakṣa they differ by exactly one month (pūrṇimānta = amānta + 1), and **10 rules are kṛṣṇa-pakṣa** — `maha-shivaratri`, `shani-jayanti`, `vat-savitri-amavasya`, `krishna-janmashtami` ×2, `karva-chauth`, `diwali`, `vassa-begins-rains-retreat`, `kathina`, `paryushana-parva-begins`. **Found by the user**, who identified that the legacy Shani Jayanti dates were right and the corrected ones wrong. Proven: Jyeṣṭha Amāvasyā is **2026-05-16 pūrṇimānta** (correct) and **2026-07-14 amānta** — which is really Āṣāḍha Amāvasyā. | `src/lib/calendar/engine.ts`, `packages/dharma-rules/src/festivals/rules.json` | **Engine fixed 2026-08-09** — both systems are now computed per day and the rule's declared system selects between them; behaviour-neutral while all rules said amānta, verified before and after. `shani-jayanti` + `vat-savitri-amavasya` set to `purnimanta` on the user's determination — corrected now **matches legacy on all three years** (16 May 2026, 4 Jun 2027, 24 May 2028). **The other 8 kṛṣṇa-pakṣa rules remain `[S]` — each needs a per-festival, per-region ruling on which system applies. 3.7 must not ship until they are decided.** |


---

## 3. Live tracker

Legend — ⬜ not started · 🟡 in progress · ✅ done · ⏸ blocked · 🔬 needs council

### ⏸️ PARKED — deliberately deferred, not forgotten (2026-08-05)

Two items gate everything downstream (profiles 5.x, panchāṅga UI 6.x, and the
3.7 switch). **Neither is an engineering task**, which is why no further prompt
moves them. Parked by explicit decision, not oversight.

| # | Parked item | Why it needs a person | To resume |
|---|---|---|---|
| **G1** | **Flip `USE_CONDITION_EVALUATOR` / `USE_CORRECTED_MASA`** (currently `false`, `engine.ts:34,40`) | Reading the shadow diff is a **religious judgement, not a technical one**. *(Correction note: previously documented as two separable independent flips; investigation revealed `USE_CORRECTED_MASA` was unread and `USE_CONDITION_EVALUATOR` hardcoded the corrected path. Now decoupled and load-bearing).* <br><br>**Māsa Correction Shift Impact (2026–2028):**<br>- 2026: 180 legacy vs 174 corrected (69 shifted, 6 removed, max shift 59d, avg shift 29.7d)<br>- 2027: 179 legacy vs 180 corrected (38 shifted, 1 removed, max shift 30d)<br>- 2028: 181 legacy vs 179 corrected (14 shifted, 2 removed, max shift 30d)<br>53 of 69 shifted rows in 2026 require the condition evaluator to settle final dates. | **Regenerate first** — `npm run diff:condition-evaluator` and `npx tsx scripts/diff-masa-correction.ts`. The committed report reflects the rules as they were when it ran. Then a human — ideally the council — reads it and decides. |
| ~~**G2**~~ | ~~Populate the 13 moonrise golden fixtures~~ | **CLOSED 2026-08-08.** Done by the user on 2026-08-07 — 11 `USNO` + 3 `HMNAO` values with `retrievedOn` stamps. This row sat stale for a day claiming the engine had "zero external validation" when it no longer did. Found by grepping the fixture file rather than reading this table — the same stale-row class already corrected at 5.4, 5.3, X2 and X3. | — |

**Safe to leave parked.** Both flags are `false`, nothing is half-applied, and
`verify:harness` is green. The work is dormant and reversible, not fragile.

> **Baseline updated 2026-08-10.** `verify:harness` now reads **574 passed /
> 666 skipped (1240)**, not the 988/216 quoted throughout the older entries
> below. That number is cited in five places as a standing tripwire and was
> stale; a review caught it. The drift is fully accountable and involves no
> regression:
>
> | | passed | skipped | total |
> |---|---|---|---|
> | old baseline | 988 | 216 | 1204 |
> | now          | 574 | 666 | 1240 |
>
> `988 − 450 newly-skipped + 36 new tests = 574`, exactly. The 450 are snapshots
> the launch-set deferral and the disputed-year withholding moved to skipped —
> deliberately, and preserving their captured values for when those rules return.
> The 36 are new suites (26 disputed-years, 10 profile-resolution). **Dated
> entries below quote the figure that was correct at the time and are left as
> written**; rewriting them would destroy the record. Compare against this row,
> not against them.
**Cost of parking:** users continue to see the current dates — Mahā Śivarātri
2026 still reads 17 Feb in the app — and 2.4 stays 🟡.


### Phase 1 — Specification

| # | Item | Status | Evidence |
|---|---|---|---|
| 1.1 | `calendar-domain-model.md` | ✅ | this commit |
| 1.2 | `astronomy-conventions.md` | ✅ | this commit |
| 1.3 | `calendar-profiles.md` | ✅ | this commit |
| 1.4 | `festival-rule-schema.md` | ✅ | this commit |
| 1.5 | `calculation-examples.md` | ✅ | this commit |
| 1.6 | `source-governance.md` | ✅ | this commit |
| 1.7 | Repo `AGENTS.md` calendar engineering rules | ✅ | this commit |
| 1.8 | Council ratification of open `[S]` items (P1–P5, Pradosha, Ekādaśī/Janmāṣṭamī methods) | 🔬 | — |

### Phase 2 — `panchanga-core` (Layer A + B)

| # | Item | Status | Blocks | Evidence |
|---|---|---|---|---|
| 2.1 | **Extract shared astronomy core + Layer-A/B boundary (this is the DEDUPLICATION item — see §7)** | ✅ | | Resolves X1 (sun/moon maths ×3), X2 (helpers ×2), X7 (entry points ×3). Extracted `src/core/astronomy.ts` (`9d5f1ba` and Stage 2 commit). |
| 2.2 | **Ayanāṁśa: true Lahiri + valid range (D7)** | ✅ | | Sourced Chitrapaksha/Lahiri formula (Positional Astronomy Centre / Indian Calendar Reform Committee 1955) in `src/core/astronomy.ts`. Range guard [1800–2100 CE], 4-epoch validation (<0.12″ residuals), 5-year 1,826-day zero-masaName-change proof. |
| 2.3 | Tolerance-based boundary solver (≤60 s) | ✅ | `2026-08-08` | **One solver: `binaryRoot`, via the astronomy adapter (rule 15).** Converged the four remaining hand-rolled bisections — `lunar-month/astronomy.ts:85`, `moon-rise-set.ts:200/217/267` — plus the two inlined angle normalisations that X2 had wrongly reported as resolved. The lunar-month loop previously returned `new Date(high)`, the **upper** bracket, i.e. up to 60 s *after* the true boundary; `binaryRoot` returns the boundary itself. Guards: `verify:harness` **988 / 216** unchanged through every swap, engine **63/63**, `tsc` clean, and the 13 USNO moonrise residuals **byte-identical** (8 neg / 5 zero / 0 pos, mean −0.62 min) across all four scan steps — confirming the swap changed convergence, not physics |
| 2.4 | **Moonrise / moonset (D14, D18–D21, D23)** | ✅ | `2026-08-08` | **Engine correct, independently verified, and now externally cited.** True-obliquity ecliptic→apparent-equatorial conversion + correct horizontal parallax in `src/core/moon-rise-set.ts`; D18–D21 resolved. **D23 closed:** the fixtures carry real Tier-1/2 citations — 11 `USNO` + 3 `HMNAO`, `retrievedOn: 2026-08-07`, entered by a person. The old "0.0 min residual across all anchors" self-derivation signature is gone; the residual distribution is now a real one — **8 neg / 5 zero / 0 pos, mean −0.62 min**, stable across all four scan steps (15 min → 15 s), with the ~37 s remainder attributed to the Meeus-vs-DE440 ephemeris floor rather than tuned away. Bisections replaced with the shared `binaryRoot` in 2.3 with the residual distribution byte-identical before and after. | 
| 2.5 | **Variable muhurta windows: Nishita, Pradosha, Madhyāhna, Aparāhna, Brahma Muhurta (D6)** | ✅ | Layer C | Extracted `src/core/muhurta.ts` (`dbcf894`). Maha Shivaratri 2026 acceptance test passed (Ujjain & Bedford). Fixed D6 in `calculatePanchang`. Note: D4 is now unblocked for item 3.2. |
| 2.6 | **Amānta/Pūrṇimānta lunar-month determination (D1/D3)** | 🟡 | **everything** | `lunar-month/` — `findAmantaMonth`, `classifyLunarMonth`, `MonthSystem`, `findNewMoon/FullMoonBefore/After`, `findSankrantisBetween`; unit + invariant tests (`7ec3f2d`, `a1bc616`, `16c14fa`). **Implementation done; 🟡 pending sourced golden validation only** |
| 2.7 | Adhika / Kṣaya māsa | 🟡 | | `classifyLunarMonth` handles both; classification corrected in `4fbd025`. 🟡 pending sourced golden validation |
| 2.8 | Solar months + regional day-assignment (P1) | 🟡 | Tamil/Malayalam/Bengali/Odia profiles | **Engine built, unwired, `[S]` throughout** (`packages/panchang-engine/src/solar-month/`). All four §2 rules implemented — `sunset_rule` (Tamil), `aparahna_rule` (Kerala), `midnight_rule` (Bengal/Assam), `same_day_rule` (Odisha) — plus the four month-name sets. `getSolarMonth`, `assignSankrantiToCivilDay`, `compareAssignments`. Sunset comes via `getSunriseForDateStr` so it inherits the §8/D21 high-latitude proxy rather than disagreeing with it. **Malayalam's Chingam offset is asserted directly** — its list starts at Siṁha, not Meṣa, and getting that wrong names every Kerala month four months out, which is plausible enough to survive review. 11 tests, built around **divergence** rather than single values: a suite that only checked one month per profile would pass with all four rules collapsed into `same_day_rule`, which is the actual risk. Every result carries `ratified: false` and an `[S]` diagnostic (rule 10 — engineering may implement and flag, never ratify). **🟡 not ✅: nothing consumes it**, and the rules need council ratification. Deferred to post-launch per the readiness plan |
| 2.9 | Era systems + true new-year roll-over (D8) | ✅ | | `5a4f341`. Rollover derived from the actual new-year event via the 2.6 lunar-month machinery, replacing a hardcoded 1 April whose own comment admitted "±14 days". Measured error of the old value: **12.94 d (2026), 5.99 d (2027), 5.81 d (2028)** — at the edge of its own admission. Seven eras modelled, reading `calendar_profiles.era`; every regional assignment `[S]` pending ratification. |
| 2.10 | **Vedic-day boundary rule (D4 partial)** | 🟡 (resolver ✅, diff report ✅ D28 resolved) | Layer C | Implemented `packages/panchang-engine/src/core/day-boundary.ts` (Version `1.0.0`) per §4 ahorātra rule. Per-user read-time resolver. **D28 resolved**: shadow diff script `scripts/diff-vedic-day-boundary.ts` now pairs occurrences by nearest instance in time (not index-0 collapse), enforces `|delta| <= 1` day harness assertion that throws on violation, and regenerates `docs/VEDIC_DAY_BOUNDARY_DIFF_REPORT.md` from script. 2026–2028: 537 total instances, 279 unchanged (52.0%), 5 Ujjain boundary corrections (0.9%), 253 diaspora locality shifts (47.1%). Nothing wired in; `verify:calendar` 995/216 unchanged. |
| 2.11 | **High-latitude policy + `latitude_proxy` flag (D21)** | ✅ | UK/Nordic users | Implemented proxy latitude $\pm 60^\circ$ recomputation for $|lat| \ge 66.5^\circ$ with `'latitude_proxy'` diagnostic tag per §8. |

### Phase 3 — `dharma-rules` (Layer C)

| # | Item | Status | Blocks | Evidence |
|---|---|---|---|---|
| 3.1 | Rule JSON schema + build-time validation | ✅ | | Validated during Next.js prebuild hook (`validate-rules.ts`) and Vitest unit testing. |
| 3.2 | **Condition evaluator incl. prevalence + `viddha` (D4 partial)** | 🟡 (D26 ✅ D27 ✅; pure evaluator, not wired) | | **D26 resolved**: canonical within-paksha tithi scheme (1..15 + paksha) documented in `festival-rule-schema.md`; `getWithinPakshaTithi(absoluteIdx)` and `isTithiMatching()` implemented and `contextPaksha` threaded into `evaluateCondition`/`evaluateVariant`; deliverable test `[D26 Fix Verification]` in `evaluator.test.ts` asserts Krishna Chaturdashi Nishita prevails on 2026-02-15. **D27 resolved**: `vitest.config.ts` now includes `src/**/*.test.ts`; `verify:calendar` = 995 passed / 216 skipped; `verify:harness` = 988 passed / 216 skipped (tripwire unchanged). Adjudication report regenerated via `npm run adjudicate-conditions` (Shivaratri, Janmashtami dual-variant ✅, Karva Chauth, Sankashti). `TithiCondition`/`PakshaCondition` type imports added — `npx tsc --noEmit` clean. Pure evaluator, not wired into engine/crons/UI. |
| 3.3 | `calendar_profiles` / `tradition_profiles` tables | ✅ | | `78f5f6f`. 11 calendar profiles (8 amānta / 3 pūrṇimānta) + 7 tradition profiles. `scholarly_status` defaults to `'[S] ratification pending'` NOT NULL, so a ratified profile cannot be created by accident. `legacy-ujjain` seeded as a real FK-valid row with `month_system` NULL — a valid target that refuses to pretend it has a month system. |
| 3.4 | **`observance_occurrences` variant qualification + uniqueness change (D15)** | 🟡 (D15 rollback ✅, rule-7 answered ✅, lock demo ✅) | variants | Migration `20260804030000_observance_occurrences_variant_qualification.sql`. Added variant identity, provenance, and computed location columns; backfilled existing rows as `'legacy-ujjain'`; replaced single-occurrence constraint with `uq_observance_occurrences_variant UNIQUE (definition_id, year, calendar_profile, variant_key)`. **D15 gaps closed**: (1) Rollback migration created `20260804030000_observance_occurrences_variant_qualification_rollback.sql`. (2) Rule 7 question resolved: `is_primary_variant` boolean cannot express read-time user profile resolution — variant identity stored per-row; UI queries match by `calendar_profile` + `spiritual_tradition` at read time. (3) `locked_for_regeneration` and `manual_date_override` guard paths demonstrated by `npm run demo:d15-lock-override` (7 fixture rows, 5 preserved / 2 regenerable, invariant verified ✓). |
| 3.5 | `source_references` table | ⬜ | governance | |
| 3.6 | Per-location evaluation (D5) | 🟡 | diaspora correctness | Prerequisite done (`7d60818`): home origin (`home_*`) separated from observance location; silent auto-overwrite replaced with detect-and-ask in `ProfileClient.tsx`. Schema unblocked by 3.4 / D15 migration. **Blocked on 2.4** — the festival pipeline (`precomputePanchangForYear(year)` / `calculateObservancesForYear(year)`) takes *no* lat/lon at all and hardcodes Ujjain at `engine.ts:58`; plumbing user location through it before D18/D19 are fixed would feed real coordinates into a calculation that is wrong by 80 min at Bedford. Daily panchāṅga **is** already location-aware (5 call sites pass user lat/lon/tz); festivals are not. Also confirm the occurrence row records `location` per AGENTS.md rule 6. |
| 3.7 | **Atomic D1+D2 migration**: month fix + 118-rule rewrite + full date diff | ⏸ | needs 2.6 | |
| 3.8 | **Alternatives + `reasons[]` in result contract** | ✅ | "Why today?" | `src/lib/calendar/observance-formatter.ts` builds the contract and groups variants. The four-way classification ([1] Dispute, [2] Uncertainty, [3] Error, [4] Location Effect) gates variant grouping: [1] requires an explicit citation in `rules.json` (D10); [4] location-only differences are never grouped as variants; [2] unresolved queue items default to 'unresolved' / 'ambiguous' status; primary chosen at read-time via user profile. Diagnostics (`latitude_proxy`, `compressed_night`, `vrddhi_tithi`, `extended_moonrise`) preserved and surfaced. |
| 3.9 | Ambiguity → review queue, never silent pick | ✅ | | Migration `20260805160000_create_observance_review_queue.sql` (`feab8e0`). Persists what was ambiguous, why, the candidate dates, the evaluator's reasoning, profile and location, using the existing `review_status` vocabulary. Prerequisite for ever flipping `USE_CONDITION_EVALUATOR` — Sankaṣṭī is 26 of 26 UNRESOLVED and Karva Chauth resolves to no date at Bedford; without this they would become silent guesses. | |
| 3.10 | Rules as data, not TS literals (D9) | ✅ | | Migrated `CANONICAL_RULES` to `packages/dharma-rules/src/festivals/rules.json`. |

### Phase 4 — Validation & governance

| # | Item | Status | Evidence |
|---|---|---|---|
| 4.1 | Golden fixture harness (D17) | ✅ | `packages/dharma-rules/`. Verified running: **988 passed / 216 skipped / 2.39 s**. Schemas enforce governance §2 (golden `tier` enum 1–4, excludes LLM; snapshot forbids `source`). Harness re-runs the real engine, not file replay. Wired into `ci.yml`. *Harness complete; sourced coverage is 4.2* |
| 4.2 | Minimum launch coverage (`calculation-examples.md` §7) | ⬜ 🔬 | 216 golden placeholders seeded with `expected: null`, `approved: false`. **Populating these is a human/council task requiring Tier 1–4 citations — engineering and AI agents must not fill them (§6).** Now the gate on Phase 3 |
| 4.3 | Astronomical validation vs Tier-1 sources, 12 cities | ✅ | `2026-08-08` | **100% cited, 12 cities, all §10 quantities.** Moonrise/moonset/sunrise 13/13 (USNO + HMNAO, human-read 2026-08-07); **sunset 13/13**, **solar longitude 8/8**, **lunar longitude 8/8**, **elongation 8/8** (JPL Horizons DE440), fetched by `npm run fixtures:fetch` straight from the authorities' APIs — provenance recorded per fixture as `retrievalMethod`. Tithi/nakshatra/Sankranti are validated at their **root** rather than against pañcāṅga tables (Tier 5, unscrapeable). Engine **104/104**. **The validation earned its keep immediately: it failed on first run and found D30** (solar ephemeris 2.2× outside budget) — plus one bug in the test itself (minute-truncation vs the authorities' rounding, which faked 6 sunset failures; all 13 were really within 30 s). Nishita is a recorded exception — no external authority exists, it is our own definition over sunrise/sunset. `npm run fixtures:coverage` prints the numbers. |
| 4.4 | Edge-case fixtures E1–E13 | ✅ | Implemented edge-case behavior test suite under `packages/dharma-rules/src/conditions/__tests__/edge-cases.test.ts` covering Adhika Masa, mock Kshaya Masa, Vrddhi Tithi, Kshaya Tithi, absent Moonrise with extension, DST transitions, Year Boundary, High Latitude proxy, and Sunrise proximity. |
| 4.5 | Persist integrity findings (D12) | ✅ | | Migration `20260806024500_create_calendar_integrity_findings.sql` (`4ff693b`). The "166 engine mismatch" figure previously existed only in a transient cron response and could not be trended or acted on. Schema records honestly that `integrity.ts` compares the engine **against itself** (D11) and so detects drift, not wrongness. |
| 4.6 | Demote AI verifier to triage-only (D13) | ✅ | | `4ff693b`. Enforced **structurally, not by comment**: `AIAllowedUpdatePayload = Omit<…, 'review_status' \| 'date' \| 'verification_status'>` plus a runtime guard — compile-time and runtime. The AI path may flag for review; it can never mark anything verified or write a published date. |
| 4.7 | Council workflow + review states live | 🔬 | |

### Phase 5 — Profiles & product surfaces

| # | Item | Status | Evidence |
|---|---|---|---|
| 5.1 | Launch calendar profiles (10) | 🟡 | Seeded and captured: the 11 profiles exist (3.3, `78f5f6f`) and onboarding collects the user's choice (5.3, `e11d54a`). **Not yet consumed** — no calculation reads the profile, because that requires `USE_CORRECTED_MASA` / `USE_CONDITION_EVALUATOR`. Data and capture done; effect gated. |
| 5.2 | Launch tradition profiles (7) | ⬜ | |
| 5.3 | Onboarding Q1–Q5 (location, region, sampradāya, scope, language) | ✅ | | `e11d54a`. Extends the existing 7-step flow; adds observance location + calendar profile, backed by the 3.3 tables. Two invariants verified: **skip means UNKNOWN, not a default** (`useState<string \| null>(null)`, null persists), and **location cannot set the calendar profile** (AGENTS.md rule 4) — geolocation writes only location fields. First work in this workstream to reach a user. |
| 5.4 | Home tradition vs observance location as separate fields | ✅ | Shipped in `7d60818` — migration `20260803020000` adds `home_latitude/longitude/city/country/timezone` + `observance_location_source`; the silent browser-timezone overwrite at `ProfileClient.tsx:516` was replaced with detect-and-ask. Tradition isolation verified (no location write can touch `tradition`/`sampradaya`, AGENTS.md rule 4); all reminder crons verified untouched. `resolveObservanceLocation` (D24/D25) then made the reference fallback explicit and kept tz travelling with its own coordinates. **Entry was stale at ⬜ until 2026-08-04.** |
| 5.5 | Local / Temple / Bharat-reference modes | 🟡 | 2 of 3. `resolveObservanceLocation` provides **Local** (device/saved) and **Bharat reference** (`REFERENCE_LOCATION_UJJAIN`, with the `isReference` UI disclosure §8 requires, D24). **Temple mode not built.** |
| 5.6 | "Why today?" explanation card | ⬜ | |
| 5.7 | **Two-recognised-observances UI + standing disclaimer** | ✅ | Implemented `ObservanceStatusNotice.tsx` component handling the two scoped states: (1) **UNDER REVIEW** (displaying candidate dates and review status without guessing) and (2) **DISPUTE** (location-conditional display of tradition variants, e.g. Janmashtami 2026 showing 2 dates at Bedford but single date at Ujjain). Includes standing geographical/tradition disclaimer matching the D24 `isReference` notice visual styling. |
| 5.8 | Travel-aware recalculation | ⬜ | |

### Phase 6 — Panchāṅga UI makeover *(deliberately last)*

Scheduled **after** Phases 2–5 so the redesign is built around correct data,
profile switching, and explanation cards rather than being reworked twice.

| # | Item | Status | Notes |
|---|---|---|---|
| 6.1 | Restyle `app/panchang.tsx` onto the design system | ⬜ | Partially begun: font/token fixes landed 2026-07-30 (`b0feaa8`). Its deliberate non-theme-reactive "living sky" design is intentional — see the file header. |
| 6.2 | Surface profile + month system in the header | ⬜ | Requires 2.6 |
| 6.3 | Amānta/Pūrṇimānta toggle with live relabelling | ⬜ | Requires 2.6 |
| 6.4 | Per-element "why / what is this" explanation | ⬜ | Requires 3.8 |
| 6.5 | Location + reference-city comparison rows | ⬜ | Requires 3.6 |
| 6.6 | Confidence & trust surface (`PANCHANG_TRUST_META`) | ⬜ | Scaffolding exists |

---

## 4. Critical path

```
2.6  Amānta/Pūrṇimānta month determination
  └─▶ 3.7  Atomic D1+D2 migration (month fix + 118 rules + date diff)
        └─▶ 3.4  Rule-variant table + uniqueness change
              └─▶ 3.6  Per-location evaluation
                    └─▶ 5.x  Profiles, onboarding, "why today?"
                          └─▶ 6.x  Panchāṅga UI makeover

4.1  Golden fixtures ── runs in parallel from day one; gates every step above.
```

**2.6 is the keystone.** Nothing about regional or sampradāya correctness can be
built before lunar months are computed correctly.

**4.1 should start immediately and in parallel.** It is the cheapest work with the
highest leverage: without cited fixtures, no later change can be proven safe, and
the existing audit cannot detect a wrong rule by construction.

---

## 5. Risks

| Risk | Impact | Mitigation |
|---|---|---|
| D1+D2 migration silently shifts published dates | Users mis-observe a fast | Atomic change; per-date diff review; user-visible change notice (`source-governance.md` §7) |
| Council capacity becomes the bottleneck | Phases 2.8/3.7/5.x stall | Batch `[S]` items; ship `global_sanatan` + `smarta` first |
| Ephemeris precision insufficient near sunrise | Wrong date on boundary days | §1.2 tolerances; Astronomy Engine upgrade path; fixtures on boundary cases |
| Swiss Ephemeris introduced without licence decision | Legal exposure | Keep the existing guard; ADR required |
| Scraping a commercial panchāṅga for validation | UK/EU database-right + ToS exposure | Tier-1 print sources, manual transcription with citation |
| Scope creep to "every observance in Sanātana Dharma" | Never ships | Phase 2 fixed at ~18 observances; profiles added incrementally |
| **Parallel old/new implementations drift apart** | A fix applied to one copy silently misses the other — the D1/D2 trap repeating | **§7 duplication register**: every duplicate is registered with an owner and an expiry gate. No duplicate may be created without one |

---

## 7. Duplication register & retirement plan

**Policy.** One concept, one implementation. Where a transitional duplicate is
unavoidable (e.g. a corrected implementation living beside a load-bearing buggy
one), it **must** be registered here with an explicit **retirement gate**. A
duplicate without a registered gate is a defect.

Audited 2026-07-30.

| # | Duplicated concept | Copies | Why it exists | Retirement gate |
|---|---|---|---|---|
| **X1** | **Sun/moon longitude** | **1** core (`src/core/astronomy.ts`) | Resolved in 2.1 (`9d5f1ba` + Stage 2) | ✅ **Resolved** |
| **X2** | Angle/ayanāṁśa helpers — `normalizeAngle`, `unwrapForward`, `lahiriAyanamsha` | **1** core (`src/core/astronomy.ts`) | **Correction (2026-08-08):** this row read "Resolved in 2.1 (`9d5f1ba`)" but was **not** true. 2.1 exported the helper; it did not remove the *inlined* copies. Two survived — `lunar-month/names.ts:61` and `lunar-month/index.ts:159` each re-implemented `((x % 360) + 360) % 360` by hand. Found and replaced with `normalizeAngle` in 2.3. Verified `0` inlined copies remain | ✅ **Resolved (in 2.3, not 2.1)** |
| **X3** | Boundary solver | **1** — `binaryRoot` via the astronomy adapter | **Correction (2026-08-08):** the prior text ("`index.ts:350` 45 fixed iterations · legacy left untouched") described a state that no longer existed — `index.ts:322` already delegated to `binaryRoot`, and it is the **live** solver for tithi/nakshatra/yoga/karana end times, never dormant legacy. 2.3 converged the remaining four hand-rolled bisections (`lunar-month/astronomy.ts:85`, `moon-rise-set.ts` ×3). Verified `0` hand-rolled bisections in the package. The `src/lib/panchang.ts:301` copy is **X8's** scope, not this row's | ✅ **Resolved** |
| **X4** | Month-name array | 2 — `MASA_NAMES` (`index.ts:82`) · `MONTH_NAMES` (`lunar-month/names.ts:40`) | Same 12 strings, **different meaning**: one labels a solar rāśi (wrong), one a lunar month (right) | **3.7** — converge only with the rule migration. Merging earlier would be incorrect |
| **X5** | Lunar month determination | 2 — legacy `masaName` (buggy, load-bearing) · `findAmantaMonth` (correct) | **Intentional, mandated.** Fixing in place would break all 118 rules | **3.7** — delete legacy in the atomic migration |
| **X6** | Festival data source | 3 — `observance_definitions`/`observance_occurrences` (live) · `festivals` table (legacy, error-fallback in 3 admin/cron routes) · `FESTIVALS_2026` static array (already `@deprecated … removed in v2`) | Historical layering | **3.10 / v2** — delete both legacy sources once rules are data |
| **X7** | Panchāṅga entry points | **1** astronomy core (`src/core/astronomy.ts`) backing thin adapters | Resolved in 2.1 | ✅ **Resolved** |
| **X8** | **`calculatePanchang` engine implementation** | 2 — `packages/panchang-engine/src/index.ts` (canonical package) · `src/lib/panchang.ts` (in-app line-for-line copy) | Historical layering before package extraction | Retire `src/lib/panchang.ts` once all app imports are migrated to `@sangam/panchang-engine` |
| **X9** | **Sadhana engine duplicate calculations** | `packages/sadhana-engine/src/content/panchang.ts` | 269 lines with its own `getTithiName`/`getKaranaName` remain duplicated even after this. | Retire once sadhana-engine delegates all panchang calculations to `@sangam/panchang-engine`. |
| **X10** | **Rule Family Metadata** | `rules.json` (authoritative) · `calendar_rule_type` in `observance_definitions` (DB representation) | The rule family defines how the engine evaluates a festival, while the DB representation exists for search/filtering. | Retire/remove the DB column or keep as cache after v2 schema transition. |
| **X11** | **Ayanāṁśa implementation** | 2 — `packages/panchang-engine/src/core/astronomy.ts:69` `lahiriAyanamsha` (canonical, sourced to PAC / ICRC 1955, validated <0.12″ at four epochs) · `src/lib/jyotish/astro-engine.ts:249` `getLahiriAyanamsa` (old linear formula, powers birth charts) | **Was an unregistered duplicate — a rule 13 defect, registered here 2026-08-08.** Not caught by 2.1 because the jyotish engine is a separate import graph from the panchāṅga engine. Population impact of converging it is already measured (`scripts/measure-real-charts.ts` → `docs/AYANAMSHA_REAL_POPULATION_REPORT.md`) | **Retirement gate:** `getLahiriAyanamsa` deleted and its callers repointed at `lahiriAyanamsha` **only** with a recorded decision on the birth-chart flips the report quantifies — a nakshatra/dasha-lord change is user-visible on a saved chart, so this is a product call, not a refactor |

### X1 is a live user-visible risk, not just tidiness

`getTodayPanchang` uses its own low-precision formulas (its comment states
**±1 tithi**) and backs the daily-digest API, while the Panchāṅga screen uses the
precise path. **The same user can be told two different tithis on the same day.**
Fixing X1 resolves this.

### Sequencing consequence: 2.1 must precede 2.2

Item 2.1 was scoped as "extract package" — cosmetic. It is not. It is the
**deduplication** item. Because `lahiriAyanamsha` exists twice (X2), doing 2.2
(true Lahiri ayanāṁśa) first means fixing it in two places, and missing one
produces exactly the silent divergence that created D1/D2. **Do 2.1 first.**

### Operational: vendored copy drift

The mobile app consumes `@sangam/panchang-engine` as a vendored tarball
(`shoonaya-mobile/vendor/sangam-panchang-engine-0.1.0.tgz`, **dated 5 Jul**) while
the source has moved on (**last modified 30 Jul**). Mobile is therefore running an
engine without the lunar-month module or the public-contract fix. Not urgent —
nothing in the app calls the new code yet — but **re-vendor before shipping any
calendar change to mobile**, and treat the tarball as a release artefact, not a
copy to edit.

---

## 6. Change log

| Date | Change | By |
|---|---|---|
| 2026-07-30 | Baseline assessment + Phase 1 specification set created | Claude (Opus 5) |
| 2026-08-10 | **E6/E7 Bedford moonrise fixture repaired from an external authority, not from engine output.** USNO's exact one-day queries publish no moonrise for Bedford (`52.135,-0.467`, UTC+1) on 2026-05-04 and publish the following night's rise at **00:19 local** on 2026-05-05. The engine returns 00:19:16 local, so the behavior and authority agree at USNO's one-minute precision. The stale fixture expected 00:18:05 from before the UT-vs-TT sidereal-time correction and was asserting unsupported millisecond precision. Replaced that brittle string with the cited USNO local minute and added an explicit assertion that the extended rise precedes the next sunrise. No engine calculation, rule, date, flag, or materialization changed. | Codex |
| 2026-08-11 | **Remediation round 3: two reviews' findings, one of which surfaced a real architecture gap that I found and reverted rather than shipped.** Both reviews independently caught real issues; every one confirmed by direct check, not assumed. **Fixed, isolated:** `maha-shivaratri`'s `ratification_note` was a copy-paste error — it declares `amanta` but the note said the 7 amānta profiles were 'in tension with this purnimanta setting' (backwards; `karva-chauth`/`diwali` genuinely are purnimanta, only `maha-shivaratri` was wrong). A test comment claimed nija Jyeshtha 'runs 06-16..06-30' — the real amānta MONTH boundary is **07-14** (verified directly); I'd conflated the pūrṇimānta NAME transition (~06-30, when the paksha flips) with the month's actual end. Root lockfile regenerated to match `panchang-engine` 0.2.1 (was still recording 0.2.0). **Attempted, found unsafe, reverted:** modeling Yogini Ekādaśī's Smarta/Vaishnava variants as two rule rows, matching the `krishna-janmashtami` precedent — surfaced that `calculateObservancesForYearCorrected`'s `occurrencesMap` is keyed by **slug only**, and its per-rule result loop never re-checks `isPublishableForYear` for each row sharing that slug; it fans one shared computed value out to every row. `sampradaya` is a display-only field the handler never reads. Setting the Smarta row's `launch_status: 'deferred'` did NOT suppress it — verified directly, both rows computed **identically** to 2026-07-11, which would have published that date labeled 'Smarta' when the source says Smarta is July 10 — **worse than the original single-row problem**, since it asserts something false rather than merely omits something true. `krishna-janmashtami`'s existing two rows carry the identical latent gap; it's never surfaced because both rows are identical in every gating-relevant field, so which one wins the shared map slot has never mattered. Currently inert — everything touching this path sits behind `USE_CORRECTED_MASA`/`USE_CONDITION_EVALUATOR`, both false — but real, and would misbehave the moment either flips. **Reverted to the single, previously-verified-correct row** rather than ship it; the Smarta date (2026-07-10) and the full gap diagnosis are preserved in the rule's own `ratification_note` for when same-slug variant rows get independently gate-able identity (e.g. keyed on slug+sampradaya, not bare slug) — real follow-up, correctly scoped as engine work, not today's fix. **Added:** two real golden fixtures (yogini-ekadashi 2026, vijaya-ekadashi 2027, `north_indian_purnimanta` @ Ujjain, `approved: false` — Tier 1 sourced, not council-ratified, so not self-approved), loaded and correctly skipped by the harness (574/**668** skipped, was 666). Council packet regenerated (was stale, zero mentions of any touched rule). **Committed the impact sweep** (`scripts/sweep-adhika-masa-collisions.ts`) rather than leave it as prose — and running it caught a real error in my OWN prior claim: the original 'Jyeshtha is ALSO a double-window hazard, three rules are correct only by luck' finding was wrong. A pūrṇimānta month name legitimately covers a śukla segment (from the following nija month) AND a kṛṣṇa segment (the '+1' rule) in every ordinary year — that's the definition of a pūrṇimānta month, not a collision. The śukla segment can never match a `tithi > 15` rule, so it was never a real hazard. Corrected the sweep to filter to kṛṣṇa-pakṣa windows only; re-run shows **zero remaining double-window hazards** across all four launch years — `shani-jayanti`, `vat-savitri-amavasya`, `apara-ekadashi` were never ambiguous, no luck involved. **Deferred, per the reviewer's own recommendation:** re-vendoring the native app's `panchang-engine` (currently 0.1.1, web is 0.2.1) until the corrected engine is actually intended to reach mobile. Guards: validate:rules 96/96, root tsc clean, verify:calendar 596/0, verify:harness 574/668 skipped | External review ×2 + Claude (Opus 5) |
| 2026-08-11 | **Fixed the adhika-māsa naming defect at its root (`packages/panchang-engine/src/lunar-month/index.ts`), resolving both previously-unresolved rules for real.** User asked why the defect wasn't caught and whether other rules were affected — both answered by tracing the actual code rather than theorizing. **Root cause**: an adhika month is deliberately assigned the same `amantaIndex` as the nija month following it (correct, §1.3), but the pūrṇimānta kṛṣṇa-pakṣa branch's `isAdhika ? x : x` prefix logic had both ternary branches returning the identical value — dead code since the module's very first commit (`7ec3f2d`), never touched by the later adhika-classification fix (`4fbd025`) because that commit only corrected the amānta side. Zero test coverage combined amanta+purnimanta+krishna-paksha, so nothing ever exercised the stub. **Swept all four launch years (2025-2028) systematically**: only 2026 has an adhika month (Adhika Jyeshtha, 17 May-16 June), producing TWO double-window masa collisions, not one — Jyeshtha as well as Ashadha. Checked every kṛṣṇa-pakṣa rule targeting either: `shani-jayanti`, `vat-savitri-amavasya`, `apara-ekadashi` all landed in the FIRST (genuine, by luck) window and were already correct; only `yogini-ekadashi` needed the SECOND window and was broken. **No other rules affected — confirmed, not assumed.** **Fix**: the krishna-paksha branch now applies the same 'Adhika ' prefix §1.3 already specifies for amānta, keyed on whether the source month is itself adhika — spec-consistent, not new policy. Verified: the two 'Ashadha' windows are now distinct strings; `LunarTithiHandler` (the real rule handler, not a synthetic probe) resolves Yogini Ekādaśī to **2026-07-11**, matching the sourced date exactly; the three already-correct rules are untouched. **`vijaya-ekadashi` also resolved** — separately, its gap was a genuine vṛddhi tithi (tithi 26 touches sunrise on both 2027-03-03 and 03-04 that year); added `corrected_prefer_last_match: true`, verified a no-op in 2026/2028 (no vṛddhi there) and selects the sourced 2027-03-04 exactly. Both rules flipped to `corrected_month_system: purnimanta` with full citations and the same pending-ratification note as the other six. 4 new regression tests added (not just my own throwaway probes) against the real 2026 Adhika Jyeshtha window, so a future ephemeris/solver change shifting the window is also caught; negative-checked — 2 of 4 genuinely discriminate against the reverted stub. ADR recorded in `docs/calendar-profiles.md` §1.3 (the canonical spec location, matching where D30's ADR actually lives — not `docs/native-adrs/`, which is native-app architecture only). `@sangam/panchang-engine` 0.2.0 → **0.2.1**. Guards: validate:rules 96/96, root tsc clean, verify:calendar 596/0, verify:harness 574/666 (unchanged), panchang-engine 119/119 incl. the 4 new tests | User + Claude (Opus 5) |
| 2026-08-11 | **Remediation of yesterday's sourcing commit — 3 malformed citations, unreproducible parser, and scope overstated on two fronts.** (1) **3 citations were literally broken**: `'listed at (matches shipping date; see docs/...)'` — my own interpolation template forgot to fill the date for the "already correct" branch. Fixed with real dates and exact PDF page numbers (7–8, 34, 57, 65, 79–81, 88, 96, 112–113). (2) **The parser lived only in `/tmp`, unreproducible by anyone.** Committed `scripts/sources/extract-panchang-dates.mjs` plus `docs/sources/rashtriya-panchang-saka-1948.manifest.md` — edition, official IMD/PAC URLs, SHA-256 of the source PDF, page references, and the zero-gap parser-verification method, all tracked; the 16.3 MB PDF itself deliberately isn't (no LFS, and source-governance.md itself prefers citing individual facts over mirroring a publication). Re-ran the committed script against a fresh extraction — reproduces all 9 dates exactly, and surfaced one I'd missed: **Maha Shivaratri has a genuine regional split in the source** — Kashmir observes it 2027-03-05, mainstream/S.India 2027-03-06 (our rule matches the mainstream reading; Kashmir isn't modelled and is noted for the record). (3) **Diagnosed, not just flagged, the two unresolved mismatches** — the reviewer's ask. `vijaya-ekadashi`: tithi 26 touches sunrise on **both** 2027-03-03 and 03-04 (confirmed via direct engine inspection) — a genuine vṛddhi tithi matching the source's own "Ekadasi ahoratra" notation; this is a day-selection question, not a system one. `yogini-ekadashi`: **2026 carries an intercalary Adhika Jyeshtha (05-17 → 06-16)**, producing two separate pūrṇimānta windows both named "Ashadha" — a short spurious one (06-01→06-15) and the genuine one (06-30→07-29, containing the real sourced date). The naive search returns the *first* match; the source's real date falls in the second. Neither `corrected_month_system` value fixes either case — both need engine-level work (vṛddhi day-selection, adhika-aware masa search) and are left unchanged. (4) **Scope was overstated on two axes.** These fixes prove the *system* for 2026/2027 dates, not every future year — adhika/vṛddhi/kṣaya can still shift a given year even with the system correct. And `corrected_month_system` is applied **globally**, not per `calendar_profile`: checked all 13 profiles — only `north_indian_purnimanta` and `nepali_bikram` (2) declare `purnimanta` matching these fixes; **7 declare `amanta` in direct tension** (`gujarati_amanta`, `kannada_amanta`, `kannada_telugu_amanta`, `marathi_amanta`, `odia`, `telugu_amanta`, `global_sanatan`); 3 are `solar` (different axis). Added `ratification_note` to all 9 touched rules stating this precisely: official-source-backed, pending council ratification, validated against the canonical/default materialisation only. `ratification_note` remains documentation-only — confirmed nothing reads it, consistent with the 2026-08-10 finding. Guards: validate:rules 96/96, verify:calendar 596/0, verify:harness 574/666 (unchanged), tsc clean | External review + Claude (Opus 5) |
| 2026-08-11 | **6 of 16 outstanding amānta/pūrṇimānta divergences sourced against a real Tier 1 authority** — *Rashtriya Panchang*, Saka 1948 (2026–27 A.D.), Positional Astronomy Centre / India Meteorological Department, English edition, provided by the user. Extracted via `pdftotext`; parser validated by requiring zero gaps across 395 consecutive day-headers (22 Mar 2026 → 20 Apr 2027) before trusting any date read from it — a month-name parsing bug (`August` vs `Aug.`) was caught this way before it could silently drop entries. **Fixed** (`corrected_month_system: amanta → purnimanta`, sourced date matches the purnimanta column exactly): `aja-ekadashi` (2026-09-07), `apara-ekadashi` (2026-05-13), `kamika-ekadashi` (2026-08-09), `rama-ekadashi` (2026-11-05), `saphala-ekadashi` (2027-01-03), `utpanna-ekadashi` (2026-12-04). **Confirmed already correct**, citation added, no code change: `karva-chauth` (2026-10-29, listed as "Karaka Chaturthi"), `diwali` (2026-11-08, listed as "Dipavali"), `maha-shivaratri` (2027-03-06 — independently matches the council ruling from 2026-08-09). **2 do NOT cleanly resolve and were not force-fit**: `vijaya-ekadashi` sourced 2027-03-04, one day off the purnimanta prediction (2027-03-03) — the source shows "Ekadasi ahoratra" the prior day, a vṛddhi-tithi pattern, so this is a tithi-boundary-selection question, not a system-naming one. `yogini-ekadashi` sourced 2026-07-10/11, matching NEITHER candidate (amanta 2026-08-09, purnimanta 2026-06-11) — off by close to a month from both, suggesting an adhika-māsa interaction the audit script's simple system+1 heuristic doesn't model. Both left as-is pending engineering investigation. **4 rules this source cannot cover**: `papmochani-ekadashi` (genuinely absent from this Hindu government publication — not spelled differently, checked exhaustively); `kathina`, `vassa-begins-rains-retreat`, `paryushana-parva-begins` (Theravāda/Jain observances, out of scope for a Hindu-tradition state panchāṅg — need a separate Tier 1–4 source). Full 16.3MB PDF NOT committed to the repo (large binary, no LFS, and our own source-governance.md endorses citing individual verified facts, not mirroring the whole publication) — citation text and the exact table rows relied on are recorded here instead. Guards: validate:rules 96/96, verify:calendar 596/0, verify:harness 574/666 (unchanged — no golden fixture references these slugs), tsc clean | User + Claude (Opus 5) |
| 2026-08-11 | **Third remediation, and the harness now runs the ACTUAL materialiser against real PostgreSQL, not hand-written SQL.** The prior '19/19' never called `materializeOccurrencesForYears` — precisely why the mass-delete defect it contained survived. **(1) Mass-delete confirmed catastrophic, and worse than described:** the retire loop `.select('id')`'d then read `.date`/`.locked_for_regeneration`/`.manual_date_override`/`.final_date_source` off the result — all `undefined` — so every branch fell through to `.delete()`. Reproducing it against the new harness showed it fires **within the very run that inserted the rows**, not only on rerun, since the retire loop runs at the end of every commit call for every batch touched. Fixed: select every field the loop reads. (2) Real Postgres surfaced a genuine bug my own harness had introduced: `node-postgres` parses `date` columns to JS `Date` objects, while PostgREST (what production actually talks to) serializes them as ISO strings, so `existingByDefinitionDate`'s keys never matched the engine's plain-string dates and the materialiser tried to insert real duplicates. **A shim whose types diverge from production produces false failures, which are worse than no shim — they look like evidence.** Fixed by pinning `pg`'s date/timestamp OID parsers (1082/1114/1184) to identity. (3) `toStamp` chose a batch by slug alone — now carries an exact `identityKey` via `batchIdentityKey()`. (4) Stamping eligibility checked only `locked_for_regeneration` — now also `manual_date_override` and `canUpdateGeneratedRow()`. (5) The claimed-dates set mixed every identity's stamps/updates into every batch's retire check — now scoped to the owning identity, with every unlink/delete error checked rather than ignored. **New harness component:** `scripts/shadow/pg-supabase-shim.mjs` (a real-SQL query builder covering exactly the chains the code calls) plus `run-materializer.mts`, which runs the real function twice and mutates REAL rows the engine just wrote — one locked, one manual-override — so the protection paths are genuinely exercised rather than tested against fixtures the code never inspects. Negative-checked: reverting the retire-loop select aborts the harness immediately (0 rows batched survive the first commit). `suppressedOverlap: 8` for 2026 from the live run matches the reviewer's independently-measured figure exactly. Guards: root 42, verify:calendar 596/0, verify:harness 574/666, rules 96, tsc clean, shadow **28/28** (19 acceptance + 9 real-materialiser) from one command; production untouched, no Supabase branch created | External review + Claude (Opus 5) |
| 2026-08-11 | **Second remediation: completeness could never be reached, and my evidence was not reproducible.** (1) `expected_row_count` was counted BEFORE named-date suppression and dedup, so it included rows the policy deliberately drops. Negative-checked: reverting the fix reds the new test with **`def-ekadashi:47/23`** — expected 47 against 23 ever persisted, a batch that could never complete no matter how often it ran. Counting now sits after policy, before the write filter, at **all four** branch points (the earlier fix had landed in the evaluator path with legacy-shaped metadata, and two branches were not counted at all). (2) Stale rows were detected but never reconciled — a leftover row meant permanently partial. Engine-generated strays are now retired; **locked, manual-override and non-engine rows are unlinked rather than deleted**, because deleting a curator's decision to satisfy a counter would be the worst possible fix. (3) Same-date rows were never stamped, so a rule/version change that kept the date left NULL batch and key forever; they are now stamped without being rewritten. (4) `legacy-ujjain` was flagged `incomplete_profile_materialisation` — it IS the fallback and all 557 rows predate the contract, so once diagnostics become visible every current observance would have carried a false warning, and a warning that always fires teaches people to ignore the one that matters. (5) **The `19/19` existed only as prose**, scripts in a scratchpad. Now `npm run verify:materialisation-shadow` — builds the shadow, applies the migration, runs 19 checks, verifies rollback, drops it. Standing rule added to the checklist as §3.7b. Guards: root **42**, verify:calendar 596/0, verify:harness 574/666, rules 96, tsc clean, shadow **19/19 + rollback** from one command; production untouched | External review + Claude (Opus 5) |
| 2026-08-11 | **Remediation of e68ab11 — and the finding that matters is about my method, not the code.** `openBatch` sent a raw-column `ON CONFLICT` against a `COALESCE(...)` EXPRESSION index. PostgreSQL cannot infer one from the other, so **the first real call would have thrown** `no unique or exclusion constraint matching the ON CONFLICT specification`. I had a real PostgreSQL shadow running and never routed the code through it — every batch test went through the fake client, which cannot model conflict-target inference. **A shadow database you do not execute against is a schema review wearing a lab coat.** Fixed to `NULLS NOT DISTINCT` on raw columns, and the acceptance harness now runs the actual upsert. Six more, all confirmed: rollback sat in `supabase/migrations/` sharing the forward timestamp (moved to `supabase/rollbacks/`, matching four siblings); the new table had **no RLS**, leaving internal materialisation history readable by any anon key (now `ENABLE`+`FORCE`, no policy, guarded `REVOKE` so it is portable — unguarded it aborted on the shadow, meaning the access-control half had never executed anywhere); `expected_row_count` was `toInsert.length`, i.e. "rows left to write", so a rerun inserting nothing would record a proud 0/0 complete — now derived from the engine's complete output; updates bypassed the helper entirely and kept NULL batch/key; produced was the payload length rather than a **count from the database**, letting a batch claim 1/1 while two rows existed. **And #6 was presence-not-cardinality for the FOURTH time** — requiring a trustworthy batch made every profile queue entry fail (queue rows have no batch and never will), so legacy always won, while my test asserted only `toHaveLength(1)`. Queue resolution is now decoupled, and the test asserts *which* entry survives. Guards: shadow **19/19** incl. the real upsert, RLS state and byte-identical rollback; root 41, verify:calendar 596/0, verify:harness 574/666, rules 96, tsc clean; production untouched | External review + Claude (Opus 5) |
| 2026-08-11 | **`sweep-adhika-masa-collisions.ts` repaired to detect the regression it claims to guard; 2025–2028 sweep re-run from the corrected script.** The prior script had two structural defects that made it blind to real collisions: (1) it pre-filtered to krishna-paksha rows BEFORE calling `contiguousWindows`, so any shukla segment silently bridged two distinct krishna fortnights — two windows became one, and the double-window hazard was never reported; (2) it never flushed the final open window at EOF, dropping any window ending on 31 Dec. Both defects confirmed to matter in practice: the Pausha double-window in 2027 is real and is now detected correctly. **Fixed**: extracted `contiguousWindows` into a pure, testable helper at `src/lib/calendar/adhika-window.ts`; the helper now iterates the complete ordered daily series, defines an active key only when `isActive(day)` is true (i.e. krishna paksha), closes/resets on non-active days, masa name changes, or EOF, and flushes after the loop. Five targeted regression tests added in `harness/adhika-window.test.ts`: (a) two same-named krishna windows separated by shukla days → two windows; (b) ordinary purnimanta shukla/krishna structure → no false collision; (c) window ending on final input day → retained; (d) previously broken bridging behavior → detected; (e) corrected sweep 2025–2028 → no unresolved rule collision. **`npx tsx scripts/sweep-adhika-masa-collisions.ts 2025 2026 2027 2028` output (verbatim):** `=== 2025 === no adhika month this year. === 2026 === adhika month: Adhika Jyeshtha [2026-05-17..2026-06-15]. === 2027 === no adhika month this year. ⚠ DOUBLE WINDOW purnimanta="Pausha": [2027-01-01..2027-01-07] + [2027-12-14..2027-12-27]. === 2028 === no adhika month this year. === rules targeting a double-window masa name: 2027 saphala-ekadashi (masa=Pausha, tithi=26) -> 2027-01-03, 2027-12-23 -- window 2 of 2 [2027-12-14..2027-12-27] (LATEST). Done. 4 year(s) swept, 1 double-window occurrence(s) found.` **Interpretation:** The Pausha double-window is a calendar-year boundary artifact — the Pausha krishna fortnight straddles the year boundary in 2027, so the slice [2027-01-01..01-07] is the tail of the previous lunar cycle and [2027-12-14..12-27] is the head of the next one. There is no adhika Pausha. `saphala-ekadashi` correctly resolves to the latest (second) window — both occurrences are real and independently publishable as recurring rules, which is the correct behavior. No unresolved collision exists. Guards: validate:rules 96/96, root tsc clean, verify:harness 585/669 skipped (4 test files, 5 new adhika-window tests all pass) | Claude (Sonnet 4.6) |
| 2026-08-11 | **Materialisation identity and completeness contract — built and proven against a SHADOW database; production untouched.** The two remaining blockers were the same mistake in different clothes: the reader guessing at a fact the writer knew and discarded. **Identity** — the formatter inferred instance identity from the date, which cannot work, because two variants of one instance fall on different days *by definition*. The producer knew all along: in `calculateOccurrencesWithEvaluator` the loop is `for (candidate)` then `for (variant)`, so every variant demonstrably belongs to one candidate. That candidate's date is now recorded as `instance_anchor` and hashed into a stored `series_instance_key`. **Completeness** — the count heuristic could not see two equally-short sets; a new `observance_materialisation_batches` table records expected vs produced, and the read path substitutes a profile only when the batch is `complete` AND the counts agree, with a DB CHECK enforcing that a batch cannot claim completeness it did not achieve. Rows with no batch (all 557) are never trusted as complete, so they can never suppress anything. Also: timezone added to both grouping identities; the two legacy insert paths were writing **neither profile nor location**, left implicit for who knows how long, now explicit. **Shadow method:** local Postgres 18.3, schema mirrored from production `information_schema`, 557 rows regenerated from a structural summary (row count, slug set, year distribution and recurring multiplicity all faithful; per-row provenance blobs are not — stated, not glossed). **12/12 shadow checks pass**, including a lying batch being rejected by the constraint and rollback restoring the schema checksum byte-identically. **Deliberately NOT done:** the route `SELECT`s are not wired to the new columns, because shipping code that expects an unapplied schema is exactly the drift that caused the missing-migration bug on 2026-08-10. Binding checklist rebaselined 988/216 → **574/666** with the arithmetic shown, and a standing instruction to show it on any future change. Guards: root **41**, verify:calendar **596/0**, verify:harness **574/666**, rules 96 valid, tsc clean; production verified unchanged (no new table, no new columns, 557 rows, 5 withheld) | Claude (Opus 5) |

| 2026-08-10 | **Annual instances collapsed across years — the SAME cardinality bug, third occurrence, third time my test missed it.** The final grouping key added a date only for recurring slugs, so annual rows stayed on `slug@lat,lon`: Diwali 2026 and Diwali 2027, which an `upcoming` window legitimately spans, grouped as competing readings of one date — both `ambiguous`, one primary. My cross-year test asserted only that both dates survived. Presence is not cardinality, and I have now made that identical mistake for recurring series, for incomplete batches, and for annual instances. Year is now in the key for **every** observance, with the date added on top for recurring series only; a converse test guards against over-splitting a genuine same-year variant pair. **Also recorded, not fixed: no diagnostic reaches a user surface at all.** `incomplete_profile_materialisation` joins `latitude_proxy`, `compressed_night`, `vrddhi_tithi` and `extended_moonrise` — all produced in `observance-formatter.ts` and consumed by nothing in `src/`, `app/` or `components/`. §8's disclosure requirement is unmet for the entire pipeline, not just the new flag, so emitting it satisfies transport and not disclosure. **Still blocking profile-qualified materialisation:** a materialiser-owned expected-count (the count comparison is a heuristic that cannot see two equally-short sets) and a stored `series_instance_key` (two variants of one Ekadashi still read as two independent primaries). Guards: root **37 passed**, verify:calendar **596 / 0 failed**, rules 96 valid, tsc clean | External review + Claude (Opus 5) |
| 2026-08-10 | **Four more findings; the two P1s were both cardinality errors my tests were blind to.** (1) **Recurring series were classified as variants of one date.** The downstream grouping keyed on `slug@lat,lon`, so a probe with three ordinary March Ekadashis returned all three `ambiguous` with only the first `isPrimary` — any consumer honouring `isPrimary` shows one Ekadashi instead of three. My own recurring test asserted `toHaveLength(3)` and never looked at status or `isPrimary`, so it passed on wrong output. Key now carries observance-instance identity; recurring variants remain inexpressible until a stored series key exists, recorded rather than guessed. (2) **'All-or-nothing completeness' was an assumption with nothing behind it** — one profile row could suppress every legacy row for a slug-year, so an interrupted materialisation writing 2 of 24 Ekadashis would silently delete 22. **My test actively endorsed this**, asserting two profile Ekadashis should replace three legacy ones. Now compares counts, keeps the fallback when the profile is short, and flags `incomplete_profile_materialisation`; a count check is weaker than a real completion marker, which the materialiser should record. (3) **Auth and profile-read failures silently downgraded the calendar** — an expired token was indistinguishable from a guest, so a stale native client was served defaults with no signal to refresh; now 401 for supplied-but-invalid credentials and 500 for a profile read fault, with PGRST116 correctly treated as an ordinary empty result. (4) **Terminal review-queue rows still published as 'under review'**; all three routes now consume only `pending_review`. Also: my 'fully green' claim on verify:calendar was luck — an invariant sits near the 5 s default and intermittently timed out; package timeout raised to 60 s. `graphify-out/` is gitignored (.gitignore:31), so it cannot appear in a commit. Guards: root **35 passed**, verify:calendar **596 passed / 0 failed**, rules 96 valid, tsc clean; both P1 fixes negative-checked | External review + Claude (Opus 5) |
| 2026-08-10 | **CORRECTION: I called the calendar-profile duplicate bug 'live and user-visible'. It is latent.** All 557 stored occurrences are `legacy-ujjain`, so there is no second row to duplicate. I had queried that same table for `spiritual_tradition` minutes earlier and never ran the one-line check on `calendar_profile` — in the same message where I praised myself for carefully separating live from latent for the sampradaya bug. The distinction I drew was invented. **Six further findings, all confirmed, five of them defects in my own fix.** (1) **Native users could not resolve a profile at all** — the routes used cookie-only auth while the app sends a Bearer token, so every native user silently got `legacy-ujjain`; now one shared `resolveRequestProfile` using `getApiUser` (cookie-then-Bearer). (2) **Resolution ran after date filtering**, so a profile row one day outside the window was invisible and the fallback published; routes now over-fetch by 31 days and the formatter clips after resolving. (3) **The grouping key lacked year** — my own defect: `slug@lat,lon` merges ~24 Ekadashi rows a year, so one profile row could delete legacy rows for dates it never covered; now (slug, year, location) with an explicit all-or-nothing completeness rule. (4) **`/month` and `/day` never filtered tradition** (pre-existing, in both the occurrence AND queue queries) while my comment claimed the SQL did — true of one route in three. (5) **Queue items were not profile-resolved.** (6) **The sampradaya lookup guard** now runs unconditionally for signed-in users, since sampradaya can never come from the query string. Also fixed the root vitest alias — `@sangam/dharma-rules` prefix-matched the subpath import and mangled it, which is why app-level calendar tests could not load. 19 new tests; negative-checked against the old code (3/7 edge, 3/4 sampradaya, 3/6 profile discriminate — stated rather than claimed as all). Guards: verify:calendar **596 passed / 0 failed**, root suite **26 passed**, rules 96 valid, tsc clean | External review + Claude (Opus 5) |
| 2026-08-10 | **Containment finished at the DATA layer, and the stored rows turned out to be stale as well as disputed.** A follow-up review pointed out that the disputed-years gate guards CALCULATION only: five contested occurrences were already in `observance_occurrences`, written before the gate existed, and every calendar endpoint kept serving them. I had called that commit containment; it was not. Three layers now: (1) **read-time** — `src/lib/calendar/withheld.ts` applied at all six occurrence readers, placed in the shared formatter so `/upcoming`, `/month` and `/day` cannot diverge; (2) **database** — new `publication_status` column plus a `disputed_ratification` ambiguity type (the three existing types all describe the ASTRONOMY being unclear, which these are not), with all five rows withheld, locked against re-materialisation, and routed to review with candidates preserved; (3) **fail-closed reads** — every path requires `publication_status = 'published'` via `.eq` on the allowed value, not `.neq` on the forbidden one, so a future withheld state hides by default. **Two rows were already `review_status = 'reviewed'`**, one admin action from notification-eligible. **All five stored dates were also stale** — legacy-path materialisations ~1 month off, matching neither the current engine nor the council (janmashtami stored 07-27 vs engine 08-25 vs council 08-24; and four alike), so they were serving wrong dates on grounds independent of the dispute. Also fixed: suppression was destroying diagnostic evidence (builders now take `applyPublicationGate`, default true); the gate had **no direct test** — a skipped snapshot is the absence of coverage, so 26 tests now assert absence AND non-over-suppression on both builders; and `disputed_years`/`derivability`/`launch_status` moved from casts into `ObservanceRule`. Separately, migration `20260806220500` was confirmed genuinely absent from production — `profiles.calendar_profile` did not exist, so the profile lookup in three calendar routes was erroring and **every signed-in user silently fell back to `legacy-ujjain`**. Applied. Guards: verify:calendar **586 passed / 0 failed**, rules 96 valid, tsc clean | External review + Claude (Opus 5) |
| 2026-08-10 | **Review CORRECTED my previous entry on three counts; all three accepted, and the gap is now enforced in code.** (1) **`ratification_note` is prose and nothing reads it.** I wrote that recording a disputed year meant it "cannot ship as silently verified". False — `isPublishable` checks only `derivability` and `launch_status`, so those dates would have materialised like any other. **Documentation is not a gate.** Added `disputed_years: number[]` and `isPublishableForYear()`, wired into both builders and both passes; verified the disputed rule-years now return **WITHHELD** on both paths. (2) **"KNOWN WRONG" was unearned.** I labelled the engine wrong wherever it differed from the council, on the council's word alone. Where a third source exists it agrees with the **engine**, not the ruling. Reclassified as *disputed*, and I am not adjudicating: per governance the council ratifies, and Drik Panchang is Tier 5 (QA signal, never a source). (3) **My evaluator claim was wrong.** `EVALUATOR_RULES` contains only `maha-shivaratri` and `krishna-janmashtami` — there is **no Paryuṣaṇa or Karva Chauth evaluator**, so "the condition evaluator handles four" was false. (4) **The kṣaya claim was wrong, and the astronomy supports neither prior answer.** Pūrṇimā 2027 is not skipped: the boundary solver puts it at **2027-02-20T02:30Z → 23:23Z**, ~21 hours, entirely within **20 February**. It is missed only because the fixed 01:00 UTC sample falls before it on the 20th and after it on the 21st. Council said 22 Feb, sampled engine said 21 Feb, astronomy says 20 Feb. Correct name is **"interval missed by daily sampling"**, not kṣaya. Guards: harness **538/666, 0 failed**, rules 96 valid, tsc clean; fallback regenerated 303 entries | External review + Claude (Opus 5) |
| 2026-08-10 | **The one-day gaps diagnosed — and suppression would do net harm.** Council added two 2028 rulings: **Guru Pūrṇimā 6 Jul 2028 — reproduces exactly**, independently confirming the Āṣāḍha correction (had the month still been Śrāvaṇa, 2028 would have landed in August). **Paryuṣaṇa 16 Aug 2028 — engine gives 17 Aug**, a fifth one-day gap. **Root cause found:** in four of the five, the ruled day is the day the target tithi *begins*; the engine samples at 01:00 UTC (≈Ujjain sunrise) and lands on the day it is *current*. None of these are sunrise-owned — Janmāṣṭamī is niśītha, Karva Chauth moonrise, Paryuṣaṇa its own reckoning. **No tithi-index tweak fixes this; that was precisely the Mahāśivarātri error.** Guru Ravidās is a **different** cause: Pūrṇimā is *kṣaya* in 2027, skipped entirely between 20–21 Feb, so the target never appears at any scan instant. **Decision: NOT suppressed, against the offered option.** Verified that **2026 is correct for all six affected rules** — Janmāṣṭamī, Paryuṣaṇa, Karva Chauth, Guru Pūrṇimā, Rakṣā Bandhan, Mahāśivarātri all reproduce their ruled 2026 dates. Suppressing would remove *correct* launch-year dates to avoid *wrong* 2027–28 ones. Instead every known-wrong year is recorded in `ratification_note` on the rule itself, with its diagnosis, so it cannot ship as silently verified. Gate: the condition evaluator must land before 2027 dates matter | Council + Claude (Opus 5) |
| 2026-08-10 | **COUNCIL RATIFICATION RECEIVED — 11 of 22 confirmed, 5 rejected, 4 off by one day.** The argument that ratification *is* the Layer B validation held: it found more than it confirmed. **(a) Guru Pūrṇimā was on the wrong month — my miss.** It sat on `Shravana|15` alongside Rakṣā Bandhan and Ullambana. **`validate:rules` flagged that exact collision and I dismissed it as "regional co-naming is normal".** It is Āṣāḍha Pūrṇimā, a month *before* Rakṣā Bandhan (ruling: 2026-07-29 vs 08-28; 2027-07-18 vs 08-17). Corrected; the collision now sits with `asalha-puja`, which is genuine cross-tradition co-occurrence on the same full moon. **(b) Mahāśivarātri's tithi-index change REJECTED, all three years.** My arithmetic was right — kṛṣṇa *N* = 15 + *N*, so Caturdaśī is 29 — but the observance falls on the day whose **night** holds Caturdaśī at niśītha, not whose sunrise does; index 29 was landing on the morning the jāgraṇa *ends*. Reverted to 28. The proper model is niśītha evaluation, not a tithi index. **(c) Four one-day gaps left UNFIXED, deliberately** — Janmāṣṭamī 2027 (−1), Paryuṣaṇa 2027 (−1), Guru Ravidās 2027 (+1), Karva Chauth 2028 (−1). All four are observances defined by a **time-of-day** condition (niśītha, moonrise, pūrṇimā instant), which is exactly what `USE_CONDITION_EVALUATOR` exists for. Hacking the dates would mask the real gap. **16 of 16 ruled dates now reproduce exactly.** New `ratification_note` field keeps each ruling — including the rejection — attached to the rule rather than only in this tracker | Council + Claude (Opus 5) |
| 2026-08-10 | **Named ekādaśīs added to the launch set — and proven blocked on the flag, not on the list.** User asked why they were deferred. Investigated rather than assumed: **only 1 of 16 can be expressed on the legacy path at all.** The other 15 are genuinely ambiguous — the legacy engine calls the *same festival's month* by different names in different years (`nirjala`: Vaishakha 2026, Chaitra 2027; `apara`'s legacy **tithi** differs, 27 vs 26). Cause: the legacy engine names months from the Sun's rāśi, which drifts against the lunation, so a kṛṣṇa-pakṣa ekādaśī falls either side of a saṅkrānti depending on the year. **No single legacy name exists to give them.** Established by measurement (`scripts/derive-legacy-masa.ts`) after first checking whether a formula would do — it would not: the legacy→corrected shift is **+2 months for 44 rules but +1 for three** (`maha-shivaratri`, `asalha-puja`, `vassa-begins`, all at or past the pūrṇimā boundary), so a fixed offset would be right mostly and quietly wrong exactly where it matters. All 16 added to the launch set as a recorded decision: **48 distinct slugs included, 47 rule rows deferred**. Verified on both paths — legacy publishes **32** (the 16 absent, as expected); corrected publishes **47**, missing only `utpanna-ekadashi`, whose Mārgaśīrṣa Kṛṣṇa date falls in January and is attributed to the following year. 0 leaks either way. **These ship the day `USE_CORRECTED_MASA` flips and not before.** Guards: harness 556/648 0 failed, rules 96 valid, tsc clean | Claude (Opus 5) |
| 2026-08-10 | **Launch subset defined; 63 rule rows suppressed. ⚠️ THE HARNESS BASELINE HAS CHANGED: 988/216 → 556 passed / 648 skipped.** New `launch_status` field, deliberately SEPARATE from `derivability` — the latter says "not knowable", this says "knowable but not chosen", and collapsing them would lose a distinction a reviewer needs. `npm run launch:apply` is the decision as data: **32 distinct slugs included, 63 rule rows deferred**, from the readiness assessment's 15 categories. "Major Sikh Gurpurabs" read as the Sikh rules already marked `kind: 'major'` — a line the repo drew, not a religious judgement made here. **Dependency closure caught a real gap:** `makar-sankranti` was not in the list but `lohri` is anchored to it, so a launch rule would have silently produced nothing; auto-included and printed. **Two defects found in my own gate while verifying:** (1) `isPublishable` was applied only to the FIRST pass, so seven deferred *relative* rules still published — `mahalaya-amavasya`, `kartik-purnima`, `sangha-day-loy-krathong`, `chintpurni-mata-sharad-navratri` and the three Jain Diwali rows — because their bases were live and the offset succeeded. Both second-pass loops now gated; verified **0 leaks, 0 missing, exactly 32 published**. (2) Suppression broke **414 snapshot fixtures**. Resolved by SKIPPING deferred snapshots rather than regenerating or deleting them: regenerating would bake "no date" into the fixture and destroy the captured value we want back when a rule returns; skipping preserves it. Mirrors the existing `[GOLDEN PENDING]` treatment. **Guard 3** added to `validate:rules`: a launch rule may not depend on a deferred base. Guards: harness **556/648, 0 failed**, engine 115, materialize-commit 7, rules 96 valid, tsc clean | Claude (Opus 5) |
| 2026-08-10 | **Product owner approved Calendar Launch Governance Batch 0.** Neutral product identity is `global_sanatan` + `unspecified`; `unspecified` may use Smārta calculation behavior but must not be labelled Smārta. Unresolved results publish no date and enter the review queue. Cited tradition disagreements remain multi-variant with exactly one primary selected at read time. Kathina and Pāvāraṇā remain outside launch. Unratified regional preferences may be stored but do not control launch calculation. `USE_CORRECTED_MASA` and `USE_CONDITION_EVALUATOR` remain disabled. This is a product/rollout decision, not `[S]` ratification and approves no changed date. Durable record: `docs/CALENDAR_LAUNCH_GOVERNANCE_DECISIONS.md`. | Product owner + Codex |
| 2026-08-10 | **2.8 solar-month engine built (Phase 2); evaluator FK mismatch fixed.** All four regional day-assignment rules from calendar-profiles.md §2, with the four month-name sets. The module exists because one Sankranti instant assigns to **different civil days** by region — which is why Pongal, Maghi, Magh Bihu and Uttarayan diverge from a single ingress. Tests are built around proving that divergence, not around single expected values, because a suite of the latter kind would pass with all four rules collapsed into one. **Separately: confirmed and fixed the review's evaluator FK claim** — `materialize.ts:153` emitted `spiritualTradition: 'vaishnava_gaudiya'`, which is not among the seeded `tradition_profiles` slugs (`smarta`, `smarta_nishita`, `nishita`, `standard`, `gaudiya_iskcon`) that the FK at migration `20260805195000:301` requires. Committing an evaluator result would have failed. Now `gaudiya_iskcon`, matching `rules.json`. **Correction to the review:** it also named `standard` as unseeded; `standard` **is** seeded and was never a problem. **Both engine flags remain `false`** — the flip was attempted on the user's instruction but blocked by the permission classifier, and left unmodified rather than routed around. Guards: harness 988/216, engine **115 passed** (104 + 11 new), rules 96 valid, tsc clean | Claude (Opus 5) |
| 2026-08-10 | **E13 repaired by re-derivation, not by loosening.** The fixture's longitude `45.117` is **synthetic** — tuned so sunrise coincides with a Chaturthī→Pañcamī boundary, to prove the engine can produce the §1.2 sub-60 s ambiguity. No observer lives there and no authority publishes it, so it is a **derived** constant, not a cited one. D30's move to VSOP87 shifted the boundary ~47 s and the gap had grown to **77.878 s**, so the fixture no longer demonstrated the case it exists for. The evidence chain: the boundary is **geocentric**, so it does not depend on the observer at all and is fixed by Sun/Moon longitudes now validated against JPL Horizons; only *sunrise* moves, at ~240 s per degree. That makes it a one-dimensional solve. `npm run derive:e13` bisects it and prints the residual. New constant **45.4416**, gap **0.007 s** — deliberately **centred**, not merely under the limit, so it retains ~60 s of headroom in both directions instead of the zero the old value had. Arithmetic checks: 45.4416 − 45.117 = 0.3246° × 240 s/° = 77.9 s, exactly the gap removed. **The 60 s assertion is unchanged** — loosening it would have deleted the test's meaning. `verify:calendar` 2 failed → **1 failed / 1009 passed**; the remainder is the pre-existing E6/E7 moonrise fixture | Claude (Opus 5) |
| 2026-08-10 | **Commit-mode tests built, then the materialiser repaired (P0).** Commit mode had **never** been exercised by a test — dry-run returns a plan and writes nothing, so no schema violation was reachable. New `src/lib/calendar/__tests__/materialize-commit.test.ts` drives it with an injected fake Supabase client that is a **constraint oracle, not a recorder**: it enforces `NOT NULL` and the D15 composite unique key on every write, exactly as Postgres would. A test that merely counted rows would have passed throughout the entire period the bug existed. **Detection verified by reverting the fix: 5 of 6 tests fail** with the real error, `null value in column "occurrence_date" violates not-null constraint`. Needed a root `vitest.config.ts` — the workspace packages resolve via tsconfig paths and `@sangam/dharma-rules` has no `main`/`exports`, so vitest could not import them. **Repair:** the existing-row map keyed on `definition_id:year`, collapsing every profile and variant into one entry. Now keys on `(definition_id, year, calendar_profile, variant_key)` — the D15 uniqueness key **minus `occurrence_date`**, deliberately: the date is the mutable part, so including it would mean a moved date never matches its stored row and every regeneration would insert a duplicate instead of updating. 7 tests pass, including a new one proving two profiles for one definition+year are no longer collapsed. Guards: harness 988/216, rules 96 valid, tsc clean; `verify:calendar` still 2 failed / 1008 passed / 216 skipped on the two classified astronomy fixtures | Claude (Opus 5) |
| 2026-08-10 | **External review received; claims verified, three fixed.** **First, a correction about my own reporting: `verify:calendar` is RED (2 failed / 1008 passed / 216 skipped) and I never ran it.** I quoted `verify:harness` (988/216) all session as "the guard"; it is a *subset* that excludes the E1–E13 edge fixtures. **Causation established** by checking out `0389ba5` (pre-D30) and re-running: **E6/E7 Bedford moonrise fails there with byte-identical values** (`23:19:16.687` vs `23:18:05.008`) → **pre-existing, not mine**. **E13 passed there** → **caused by D30**. The arithmetic fits: D30 shifted solar longitude ~24″, elongation moves 12.19°/day, so a tithi boundary moves ~47 s — a fixture at ~31 s becomes 77.878 s. E13's own docstring says it cannot catch ephemeris error "because the test itself queries Astronomia", which is precisely what happened. **Fixed:** (1) `Cache-Control: public` → `private` on `day`/`month`/`upcoming` — these infer profile and tradition from the signed-in user, so a shared CDN keyed on URL alone could serve one user's calendar to another; `export` was already `no-store`. (2) **P0** — both legacy-path inserts omitted `occurrence_date`, `NOT NULL` since D15 migration line 93, and the legacy path is the ACTIVE one while `USE_CONDITION_EVALUATOR` is false, so commit-mode materialisation would fail on insert. (3) The legacy UPDATE changed `date` without `occurrence_date`, leaving rows keyed on one date and claiming another. (4) Unresolved rows leaked a legacy-engine *guess* through the backward-compatible `date` field; now blanked for all unresolved rows, not just `engine_error`. **Verified, NOT fixed:** the existing-row map keys on `definition_id:year`, discarding `calendar_profile` and `variant_key` — harmless today (one variant per definition) but wrong the moment multi-profile materialisation starts. Left because the review is right that it needs commit-mode tests first, and none exist | External review + Claude (Opus 5) |
| 2026-08-10 | **Kathina + Pāvāraṇā withdrawn from publication (user decision).** Shoonaya ships no Theravāda calendar profile, so an amānta-derived date for either is an India-derived substitute, not the observance's date. Added `isPublishable()` to the engine, gating on `derivability`: a rule that is not `computed` emits **no occurrence**, which removes it from calendars, materialisation and notifications in one place — all three read `observance_occurrences`, verified. Definitions, sources and `derivability_note` remain in `rules.json` so they can be switched on the day a real profile lands. Verified: both produce `(none)` on **both** legacy and corrected paths, 2026 and 2027. No golden/snapshot fixture referenced either, so `verify:harness` stays **988/216**. **⚠️ Coherence gap now open:** `asalha-puja` (2026-07-29) and `vassa-begins-rains-retreat` (2026-07-30) still publish India-derived dates while Pāvāraṇā — **the end of that same rains retreat** — is suppressed. The calendar now says when Vassa begins and nothing about when it ends. Four more Theravāda/Tibetan rules are in the same position: `vesak-buddha-purnima`, `magha-puja`, `sangha-day-loy-krathong`, `losar-tibetan-new-year`. Awaiting a decision on the class | User + Claude (Opus 5) |
| 2026-08-10 | **Correction (user): the Kathina verdict was overstated, and its reasoning was circular.** I wrote "proven underivable" and "25 Oct precedes Vassa". Both are wrong. I computed Pāvāraṇā at 26 Oct **with our amānta engine** and then treated that as the fixed point disproving a Theravāda date — using a Hindu profile to adjudicate a Theravāda observance, which is the very thing in question. The amānta/pūrṇimānta disagreement shows only that **our profiles cannot derive it**, not that it is underivable. Nor does the astronomical full-moon instant (`2026-10-26T04:12Z`) by itself fix a ritual civil date. **Sri Lanka's official 2026 calendar places Vap Full Moon Poya on 25 Oct** (Central Bank of Sri Lanka, user-provided), while other Theravāda calendars place Pāvāraṇā on 26 Oct — profile-dependent, not wrong. And the point I under-weighted most: **Kathina is a season, not a day** — the month after Pāvāraṇā, within which each monastery picks its own date — so no calendar yields a single correct Gregorian date. **New `derivability` field** added to the rule schema, splitting the two because they fail differently: `pavarana-end-of-vassa` → `requires_tradition_profile` (computable once Sri Lankan / Thai / Burmese profiles exist); `kathina` → `externally_curated` (no profile will ever fix it; the date must come from outside). Rules otherwise unchanged, per the user's instruction | User + Claude (Opus 5) |
| 2026-08-09 | **Council packet built; `[3]` ERROR closed; Kathina proven underivable.** (1) `docs/COUNCIL_RATIFICATION_PACKET.md` (`npm run council:packet`) — plain-language, one row per changed date with a derived reason, no code. **The real count is 13 dates in 2026 and 14 in 2027, not the 69 originally quoted or the 28 I quoted after the D32 fixes** — that 28 still counted the new ekādaśīs as *moved*, which they stopped being once their legacy month name was removed. (2) **`[3]` ERROR resolved as an operational state, not a fourth user-facing one.** Added `engine_error` to `ambiguity_type`. It is never set by the engine — an engine cannot know it is wrong, which is precisely why external validation exists — but by a reviewer, which the 4.3 Tier-1 harness now makes possible. It **withholds the date**: `civilDate` was already null, and the legacy `date` field is now blanked too, since that field carried exactly the value known to be wrong. Deliberately kept to three user-facing states — to a user, uncertainty, location effect and our error all mean *this date is not confirmed*. (3) **Kathina — see the correction in the next row; the verdict recorded here overstated the evidence.** | Claude (Opus 5) |
| 2026-08-09 | **Two findings from a post-batch sweep.** (1) **Mahāśivarātri's +1 day is not a day-boundary bug — it is a deliberate off-by-one correction, and the corrected value is right.** The rule carries `lunar_tithi_index: 28` (legacy) and `corrected_lunar_tithi_index: 29`. Both paths report the same tithi on the same day, so the shift is purely the index. The scheme is established in-repo by the generic `ekadashi` rule's `recurring_tithi_indices: [11, 26]` — śukla *n* = *n*, kṛṣṇa *n* = 15 + *n*. So kṛṣṇa Caturdaśī = **29** and kṛṣṇa Trayodaśī = 28. Mahāśivarātri is definitionally Kṛṣṇa Caturdaśī, so legacy targets Trayodaśī and fires **a day early**; corrected is correct. Cross-checked: the only tithi-30 rules are the two Amāvasyā observances, consistent with 30 = Amāvasyā. **No code change needed** — this folds into the ratification set rather than being a separate investigation. (2) **All 216 golden fixtures are `approved: false`, so every one is `it.skip`ped.** The harness's "988 passed / 216 skipped" therefore means the *snapshot* layer is green and the **correctness layer is entirely dormant** — zero golden assertions have ever run. The mechanism is honest (`harness.test.ts:396-407` runs only approved fixtures and refuses to treat a placeholder as passing), but the practical coverage is nil, and 18 of 95 rule slugs have even a placeholder. Recorded, not fixed — approving one requires a cited date, not an engine change | Claude (Opus 5) |
| 2026-08-09 | **Live defect caused by my own prompt, found and fixed within the hour.** The 16 new named-ekādaśī rules were authored with `lunar_masa_name` set equal to `corrected_lunar_masa_name`, because my prompt said to do that and claimed "the legacy path will not resolve these". **That claim was wrong** — `LunarTithiHandler` matches on the field regardless, so the legacy path resolved all 16 at D1-shifted dates. `USE_CORRECTED_MASA` is `false` and materialisation dispatches on it, so **Kamada Ekādaśī would have published as 2026-05-26 instead of 2026-03-29** — about two months out. Found by re-counting the corrected-vs-legacy diff after the day's fixes and noticing the new rules appearing as *moved* (which requires BOTH paths to resolve them). Fixed by removing `lunar_masa_name` from the 16; the handler returns `[]` without it, so legacy now publishes **0 of 16** rather than 16 wrong dates. Corrected path unchanged. **Guard added:** `validate:rules` now hard-fails when `lunar_masa_name === corrected_lunar_masa_name` — **0 of the 47** rules carrying both names have them equal, so equality is a reliable signature of the corrected value being copied into the legacy field. Guards: harness 988/216, engine 104/104, rules 96 valid, tsc clean | Claude (Opus 5) |
| 2026-08-09 | **D32 — the 69-date migration was about to ship month-late dates, and the user caught it.** Asked for Shani Jayanti under both systems; the answer shifted in *all three* years (+59/+30/+29), not just the leap-month year — which contradicted my earlier explanation that 2026's Adhika Jyeṣṭha was the cause. User stated the legacy dates are the correct ones. Root cause is **not** the astronomy: `corrected_month_system` was never read, so every rule was evaluated as amānta regardless of what it declared, and all 47 were defaulted to `'amanta'` anyway. Vat Savitri / Shani Jayanti is a **North Indian, pūrṇimānta** observance — the rule's own description says so. Engine now honours the field; the two rules are corrected and match legacy exactly. **Corrected-vs-legacy differences dropped from 35/35/6 to 33/33/4.** 8 kṛṣṇa-pakṣa rules still undecided and now block 3.7. Guards: harness 988/216, engine 104/104, rules valid (80), tsc clean | Claude (Opus 5) + user |
| 2026-08-08 | **4.3 CLOSED — and the external validation immediately paid for itself.** Fetched all 21 outstanding fixtures directly from USNO/HMNAO and JPL Horizons via `npm run fixtures:fetch`; provenance recorded honestly as `retrievalMethod: 'automated-fetch'` rather than claiming a human read them. **First run: 11 failures.** Six were a bug in *my test* — it truncated the engine time to the minute while the authorities round, faking a −1 min error; all 6 had engine-seconds ≥30 and all 7 passing cases did not, a perfect match. Real sunset agreement is **13/13 within 30 s**. The other five were real: **D30 confirmed against JPL** (24.0″ vs the 12.2″ budget), independently agreeing with the earlier VSOP87 measurement. **D30 fixed** — Sun moved to `solar.apparentVSOP87`, ADR written, package bumped to 0.2.0. That surfaced **D31**, a pre-existing `unwrapForward` defect making `findNewMoonAfter` return null for ~50 min after every new moon, masked for who knows how long by a 0.005° guard. Both fixed. Engine **104/104**, harness **988/216 unchanged**, tsc clean | Claude (Opus 5) |
| 2026-08-08 | **D30 found: we are outside our own tolerance budget, and the fix is already installed.** Asked whether astronomia could supply the 4.3 reference values. It cannot — our engine *is* astronomia, so that would be a self-comparison, the exact D23 signature. But the question surfaced something better: astronomia ships **full VSOP87 and ELP/MPP02 with data files present**, while we call the *low-precision* Meeus series for both bodies. Measured: solar **2.23× over** the Sankranti budget, elongation **1.08× over** the tithi budget; Moon comfortably within. The moonrise residual previously attributed to an inherent "ephemeris floor" was therefore **optional, not inherent**. Date impact measured across 1096 days: **1 index changes, 0 festivals move**. Left unfixed as a `[C]` decision | Claude (Opus 5) |
| 2026-08-08 | **4.3 harness built** for the five §10 quantities left uncovered (sunset, tithi, nakshatra, Sankranti, Nishita). Design decision: rather than chase published pañcāṅga tables (Tier 5, unscrapeable), validate the **two longitudes all three lunar/solar quantities derive from** against **JPL Horizons**. Encodes the aberration trap explicitly — apparent vs astrometric differ by ~0.0057°, which is **larger than the 0.0034° Sankranti budget**, so the wrong column would inject more error than it measures while looking like a valid citation; `frame` is a required field and the test asserts it. Nishita recorded as an exception with no external authority rather than faked. 21 fixtures outstanding, printed by `npm run fixtures:coverage`. Guards 988/216, tsc clean | Claude (Opus 5) |
| 2026-08-08 | **Task 1 reviewed + repaired; Task 2 (5.7 UI copy) done — 2 defects fixed.** **Task 1 defect:** the gate wiring was correct, but `calculateObservancesForYear` dispatching on the gate meant `scripts/diff-masa-correction.ts` — which diffs it against `…Corrected` — became a **self-comparison** the moment the gate flipped. Measured: 2026 Legacy-vs-Corrected = **74 differing pairs with the gate off, 0 with it on**. The flip destroyed the only tool that could review the flip. Fixed by adding `calculateObservancesForYearLegacy` (gate-independent) and repointing `diff-masa-correction`, `diff-vedic-day-boundary` and `diff-condition-evaluator` at explicit paths; `scripts/verify-masa-gate.ts` added as a tripwire that exits 1 if the distinction collapses again. Re-verified: **74 with the gate ON**. Also renamed the formatter's `legacyDate` → `fallbackDate` (it holds a gate-dependent value). **Task 2 defect A:** `observance-formatter` sets `status: 'ambiguous'` for the [2]/[3]/[4] cases, but `ObservanceStatusNotice` only tested `'unresolved'` — ambiguous fell through and rendered identically to a **confirmed date**. Engine detected the conflict; UI dropped it. New 'Date Not Confirmed' state added, worded to name **no tradition** (cause is unknown and usually location). **Task 2 defect B:** the variant group key was `festivalId` alone while none of `calendar/day|month|upcoming` constrain location — so rows computed at different locations could group together, count as two traditions, and publish as a **[1] DISPUTE**. That is the Janmāṣṭamī error made reachable in code. Key now includes lat/lon. **Known gap (not fixed):** `[3]` ERROR has no distinct status — statuses are only `resolved|ambiguous|unresolved`, so the four-way classification is three-way in the formatter and three-state in the UI. Guards 988/216, 63/63, tsc clean throughout | Claude (Opus 5) |
| 2026-08-08 | **Gate flip attempted under user approval — STOPPED, gates are not what this tracker said.** (a) `USE_CORRECTED_MASA` is **read by nothing** — only its own definition and comments. Flipping it is a **no-op**; the wiring its doc comment describes (swapping `precomputePanchangForYear` → `precomputePanchangCorrectedForYear`, repointing rules at `corrected_lunar_masa_name`) does not exist. (b) `USE_CONDITION_EVALUATOR` is live but **entangled**: `calculateOccurrencesWithEvaluator` calls `calculateObservancesForYearCorrected` unconditionally (`materialize.ts:302`), so flipping the evaluator silently flips corrected-masa too — two changes documented as independent, delivered as one. (c) `observance-formatter.ts:81` already calls the corrected path unconditionally and names the result `legacyDate` (:153), so the client fallback and the published date come from different engines. **Cost of the flip, measured** (`scripts/diff-masa-correction.ts`): 2026 **69 of 174 dates shift**, avg 29.7 d, max 59 d, 6 removed; 2027 38 shift / 1 removed; 2028 14 shift / 2 removed. `shani-jayanti` and `vat-savitri-amavasya` both move 16 May → 14 Jul. 53 of the 69 are `NEEDS_MUHURTA_EVAL`. **No gate was flipped.** Prompt to make the gates real: `docs/prompts/NEXT_BATCH.md` Task 1 | Claude (Opus 5) |
| 2026-08-08 | **2.3 closed — one boundary solver.** Four hand-rolled bisections replaced with `binaryRoot` through the astronomy adapter; two inlined angle normalisations replaced with `normalizeAngle`. Package now has **0** hand-rolled bisections, **0** inlined normalisations, and imports `astronomia` in exactly **1** file (rule 15). **Two §7 rows were wrong and are corrected in place:** X2 claimed "Resolved in 2.1" while two inlined copies survived in `lunar-month`; X3 described `index.ts:350` as dormant 45-iteration legacy when it already used `binaryRoot` and is the live tithi/nakshatra/yoga/karana solver. **X11 registered:** the jyotish `getLahiriAyanamsa` was an unregistered duplicate of the canonical ayanāṁśa — a rule 13 defect, missed by 2.1 because the jyotish engine is a separate import graph. Guards held at 988/216 and 63/63 through every swap; moonrise residuals unchanged | Claude (Opus 5) |
| 2026-07-30 | Lunar-month module landed (2.6/2.7/2.3 → 🟡); golden harness landed and verified (4.1 → ✅, 988 pass / 2.39 s). Review found and fixed: unreported `package.json` regression breaking `corpus:validate-indexes`, and an unmemoized engine call making the suite unrunnable (8.1 min → 2.39 s). Verified `masaName` output byte-identical after an 845-line `index.ts` refactor — the D1/D2 landmine held | Claude (Opus 5) + Antigravity |
| 2026-08-03 | Muhurta engine landed and verified (2.5 → ✅, D6 resolved); Maha Shivaratri 2026 window test passes at Ujjain and Bedford with genuinely variable night lengths. Location model landed (`7d60818`): `home_*` vs observance location separated, silent timezone overwrite removed, tradition isolation and reminder crons verified untouched | Claude (Opus 5) + Antigravity |
| 2026-08-03 | **2.4 reverted ✅ → 🟡 after independent review.** Moonrise shipped with 8/8 green tests but is wrong by up to 80 min (Bedford) — §10 allows 2 min. Root causes filed as **D18** (ecliptic λ/β read as RA/dec), **D19** (`parallax(jde)` given a Julian Day instead of km), **D20** (24 h civil day breaks on DST), **D21** (`latitude_proxy` flagged but §8 proxy never computed). **D23** files the meta-defect: the suite tested invariants, not absolute accuracy, so shared-frame errors cancelled and passed. **D22** filed separately: `calculatePanchang` silently defaults to London. Findings originated with an independent Codex review; each was reproduced here from source before filing — one Codex claim (that §8 forbids a `null` civil-date moonrise) was checked and rejected, §8 requires it. No festival date is currently affected: nothing consumes moonrise yet | Codex (found) + Claude (Opus 5, verified) |
| 2026-08-04 | **D22 resolved.** Removed default parameters from `calculatePanchang` in both `packages/panchang-engine` and `src/lib/panchang.ts`. Exported `REFERENCE_LOCATION_UJJAIN`. Enforced explicit location parameters across all call sites (`NityaKarmaClient`, `PathshalaClient`, `HomeDashboard`, `panchang/page.tsx`, `panchang/today/page.tsx`, etc.). Registered `src/lib/panchang.ts` duplicate as **X8** in §7. `npx tsc --noEmit` and `verify:calendar` (988 passed / 216 skipped) clean. | Antigravity |
| 2026-08-04 | **Review of the 2.4 remediation and D22.** D18–D21 confirmed genuinely fixed (anchors reproduce; proxy really recomputes at lat 60° with the §8 66.5° threshold; `astronomy.ts` untouched; `masaName` and tripwire unchanged). D22's core accepted: defaults removed, compiler-enforced, `NityaKarmaClient` live bug fixed, X8 correctly found and registered. **Three corrections applied to the record and filed as follow-ups:** (a) **D23 reverted Resolved → Open** — fixtures are self-derived with fabricated USNO provenance, and **2.4 reverted ✅ → 🟡** on that basis; (b) **D24** filed — `REFERENCE_LOCATION_UJJAIN` used as a silent fallback in `HomeDashboard`/`PathshalaClient`, violating its own §8 labelling contract (D22 moved the silent default from London to Ujjain rather than eliminating it); (c) **D25** filed — timezone/coordinate pairs now incoherent at those two sites. | Claude (Opus 5) |
| 2026-08-04 | **D24 & D25 resolved.** Created exported `resolveObservanceLocation(input)` resolver returning `ResolvedLocation` (`lat, lon, tz, source, isReference, label`). Landed identically in `packages/panchang-engine/src/index.ts` and `src/lib/panchang.ts` per X8. Coherently binds coordinates with their matching timezone. `HomeDashboard` and `PathshalaClient` updated to resolve location/timezone as a single unit and render non-dismissible §8 Bharat reference disclosure banners whenever `isReference` is true. `npx tsc --noEmit` and `verify:calendar` (988 passed / 216 skipped) clean. | Antigravity |
| 2026-08-04 | **D15 / Tracker 3.4 resolved.** Created migration `20260804030000_observance_occurrences_variant_qualification.sql`. Added variant identity (`calendar_profile`, `spiritual_tradition`, `variant_key`, `is_primary_variant`), provenance (`rule_version`, `astronomy_version`, `day_boundary_version`, `reasons`, `source_refs`, `diagnostics`), and computed location (`computed_latitude`, `computed_longitude`, `computed_timezone`) columns to `observance_occurrences`. Backfilled existing rows as `'legacy-ujjain'`. Replaced single-occurrence constraint with `uq_observance_occurrences_variant UNIQUE (definition_id, year, calendar_profile, variant_key)`. Regenerated `src/types/database.ts`. `npx tsc --noEmit` and `verify:calendar` (988 passed / 216 skipped) clean; zero stored dates modified. Unblocks D10 (Janmashtami variants) and 3.6 (location evaluation). | Antigravity |
| 2026-08-04 | **Tracker 2.10 / Defect D4 (partial) landed (Shadow Mode).** Created `packages/panchang-engine/src/core/day-boundary.ts` (`DAY_BOUNDARY_VERSION = '1.0.0'`) per §4 ahorātra rule. Implemented per-user read-time Vedic day boundary resolvers (`resolveVedicDayForInstant`, `resolveVedicDayForInterval`). Executed Shadow Mode diff script `scripts/diff-vedic-day-boundary.ts` and committed report `docs/VEDIC_DAY_BOUNDARY_DIFF_REPORT.md` across 2026–2028 (234 observances: 42.7% unchanged, 13.2% Ujjain boundary corrections, 44.0% diaspora locality shifts). Nothing wired in; `verify:calendar` 988/216 unchanged; unit tests 48/48 passed. Tracker 2.10 → 🟡. | Antigravity |
| 2026-08-04 | **Tracker 3.2 / Defect D4 (partial) landed (Pure Evaluator).** Created `packages/dharma-rules/src/conditions/` (`CONDITION_EVALUATOR_VERSION = '1.0.0'`). Evaluates tithi/nakshatra position, prevalence (`at`, `prevails`, `touches`, `majority`), muhurta/moonrise windows, and `viddha` conditions with detailed structural `reasons[]` and diagnostics. Executed Adjudication script `scripts/adjudicate-conditions.ts` and committed report `docs/ADJUDICATION_REPORT.md` (Shivaratri, Janmashtami dual-variant, Karva Chauth, Sankashti). Pure evaluator; zero engine wiring; `verify:calendar` 988/216 clean. Tracker 3.2 → 🟡. | Antigravity |
| 2026-08-04 | **D26, D27, D28 resolved; D15 gaps closed (Tracker 3.2, 2.10, 3.4).** **D26**: canonical within-paksha tithi scheme documented in `festival-rule-schema.md`; `getWithinPakshaTithi` + `isTithiMatching` + `contextPaksha` threading implemented in evaluator; deliverable Krishna Chaturdashi test passes. **D27**: `vitest.config.ts` includes `src/**/*.test.ts`; baseline `verify:harness` = **988 passed / 216 skipped**; `verify:calendar` = **995 passed / 216 skipped**. **D28**: diff script `scripts/diff-vedic-day-boundary.ts` now pairs by nearest instance in time, enforces `|delta| <= 1` day assertion that throws loudly on violation, and regenerates `docs/VEDIC_DAY_BOUNDARY_DIFF_REPORT.md` from script; 2026–2028: 537 instances, 279 unchanged, 5 Ujjain corrections, 253 diaspora shifts. **D15 rollback** migration created. **D15 Rule 7**: resolved — `is_primary_variant` cannot express read-time profile resolution; variant identity stored per row, queries match at read time. **D15 lock demo**: `scripts/demo-d15-lock-override.ts` demonstrates all 3 guard paths, 5/7 rows preserved, invariant ✓. `TithiCondition`/`PakshaCondition` imports fixed — `npx tsc --noEmit` clean. | Antigravity |
| 2026-08-04 | **Reconciliation remediation landed, and a correction to the record.** Resolved: the Karva Chauth prose/table contradiction, the hardcoded §3 literal (`reconcile-engines.ts:241`), the missing §8 next-night moonrise extension (`evaluator.ts:130-133`, reporting the actual instant per §8), the binary→three-state Disagreement column (256 comparisons, 29 YES), and the `NEEDS_MUHURTA_EVAL` reclassification. **Two findings surfaced rather than solved:** Karva Chauth at Bedford resolves to *no date* even at ±35 days — a major vrata uncomputable for diaspora users, which is a council question, not a window-widening one; and Sankaṣṭī moved from ~10-of-12 disagreeing to **26 of 26 UNRESOLVED**, i.e. the metric got cleaner while nothing got settled. **Correction:** commits `2780330` and `f9e5419` already contained this remediation, but their messages describe the pre-fix state and list four already-fixed items as "known open" — staged from an earlier review without re-reading the files at commit time. Filed as REVIEW_CHECKLIST §3.9. | Claude (Opus 5) |

### Note on snapshot fixture count

The 972 snapshot fixtures are **54 distinct assertions with 18× redundancy** — all
18 location/profile combinations currently return an identical date, because the
engine has no profile support (D3) and computes everything at Ujjain (D5). This is
expected and is documented inside each fixture.

Their value is as a **tripwire**, not as 972 independent checks: once D5/D3 land,
these combinations *must* diverge, and if they don't, the fix didn't work. The
tripwire has already proved itself once — it confirmed that the 845-line
`index.ts` refactor on 2026-07-30 changed zero festival dates.
